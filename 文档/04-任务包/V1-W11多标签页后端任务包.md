# V1-W11/r1 多标签页后端接入

主控已核对上游最终接口并派发。Runner W10/r1已交付，协议与真实Chrome基础流程通过；Backend必须先完成A1并无活动轮次再发送。按开发优先，只基本检查。

WebCase资产增加NEW_TAB、SWITCH_TAB、CLOSE_TAB。NEW_TAB严格type/url/value/timeout_ms/failure_policy，url按既有http(s)/合法模板解析语义，value稳定别名；SWITCH/CLOSE严格type/value/timeout_ms/failure_policy。不用浏览器索引、不新增其他字段。别名[A-Za-z][A-Za-z0-9_-]{0,63}，NEW_TAB不能main。执行计划仍type/timeout_ms/failure_policy/url/locator/value/key七字段，不适用项显式null，schema_version=1。

按W10契约main为初始页、新页创建后切换、最多16个受管页；未知/重复/最后一页关闭由Runner现场受控判定，后端不编造执行成功。不自动注册业务popup。准确保留不可变WebCaseVersion、ElementVersion、Runtime模板、预算、failure_policy及完成报告节点身份。

允许backend/app/modules/web_cases/schemas.py、runs/service.py仅必要动作计划/模板/白名单局部、相关backend/tests。禁止改认证A1、迁移、报告快照语义、Runner/Frontend和正式服务。Runner冻结输入以W10报告最终指纹为准。

只编译/lint及合法/非法形状和一条隔离HTTP锁版本→plan→真实Runner解析检查，不全量/Chrome/重复主控组合。A1引入真实身份fixture后沿新fixture运行，不绕过认证来使测试通过。

独占.codex-validation/v1-w11/、文档/05-交付记录/V1-W11多标签页后端交付.md。短报告列修改、基本检查、接口、未验项并结束；不跨任务消息、等待、Git、Claude、内部Agent。