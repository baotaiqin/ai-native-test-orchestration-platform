# P6-I1/r2 主控集中审查修订

主控已确认 r1 completed/idle，结束游标 `caa7ffb1-2ae4-4fe2-a9ec-fd3e44e0fe48:25`。r1 Backend 全量444项通过，主控核对交付14项指纹，独立15项为10通过、5失败。本包在上一轮结束后一次性派发，不是运行中追加意见。

## 修正以下最终确认问题

1. **ACTIVE唯一性可被NULL绕过。** 实际模型CHECK允许 `status=ACTIVE, active_slot=NULL, removed_by=NULL, removed_at=NULL`，表达式结果UNKNOWN使数据库接受。主控 `test_requirement_link_constraints_review.py` 直接执行生产CHECK文本复现1项失败。模型和0039迁移同时明确ACTIVE slot非NULL且为1；保留REMOVED审计完整和多个历史记录语义。验证直接SQL异常形态及正常状态，不仅服务层默认值。
2. **新关联时间不符合UTC契约。** `test_requirement_link_api_review.py` 显式新增created_at、移除removed_at、手工创建附带关联created_at三项失败，HTTP JSON无offset；手工/AI接受路径新链接还使用数据库func.now()，受连接时区影响。所有新关联写入可靠UTC并以明确offset输出，覆盖显式创建/移除、手工创建、合成AI建议接受及旧兼容链接接口。旧created_at来源不明，禁止直接补Z、按created_by或id猜测UTC、统一减8小时或改写旧数据；采用明确可靠的新旧来源语义（必要时本迁移新增标记），让调用方能区分旧未知并保留原历史表示。removed_at是本迁移新增字段，新值可按可靠UTC约定处理。只修关联审计，不扩大为全项目时间改写。
3. **MySQL外键名称超限。** `test_requirement_link_mysql_compile_review.py` 根据实际MySQL离线DDL及dialect.max_constraint_name_length验证失败：`fk_requirement_case_links_supersedes_link_id_requirement_case_links`为67字符，上限64。短CHECK名称已修，但此FK仍需修正；模型、创建和删除名称保持一致，包括Alembic命名约定和op.f。不要用“能生成SQL”代替标识符有效性。
4. **迁移降级依赖顺序。** 当前downgrade先drop_index(supersedes/web_case_version/web_case/case_version)，后drop对应FK，存在MySQL拒绝删除依赖索引的风险；这是主控静态审查，尚未声称真实MySQL失败。按先解除依赖外键、后删除所属索引顺序修正，保持有新语义数据时在任何破坏DDL之前安全拒绝；核对其他unique/索引/FK依赖及空/旧兼容非空路径。真实MySQL由下一I1M包执行，不反向等待。

手工创建对归档需求的预审问题已在r1最终代码修复，主控探针通过，本包保留该修复即可；Diff6项也已通过。不要把这两个已通过问题重复列为未修。

## 范围与验证

继续r1同一产品范围；允许修订尚未正式应用的0039及相应模型、关联服务/schema、局部test_cases关联创建与测试。不改旧迁移、Runner/Frontend、reports/runs、正式服务/数据库或主控探针。Runner独立V1-W1正在修改Runner文件，禁止交叉写入或消息联系。

主控四个探针路径：

- `.codex-validation/test_requirement_diff_review.py`
- `.codex-validation/test_requirement_link_constraints_review.py`
- `.codex-validation/test_requirement_link_api_review.py`
- `.codex-validation/test_requirement_link_mysql_compile_review.py`

修复后运行这15项、Backend全量和Ruff，并新增有意义的UTC新旧/实际CHECK/迁移兼容回归。仅隔离SQLite及离线DDL，无正式凭据/业务/AI/服务重启。不要通过修改主控测试隐藏失败。

保留r1结果副本在自己的`.codex-validation/p6-i1/`，更新最终result及《P6-I1需求关联与影响后端交付》，包含新旧时间API语义、命令/结果/指纹和真实MySQL仍待验边界。主控审查JSON为只读。遵守AGENTS主控独占调度，禁止任何跨任务工具/Git/Claude/内部Agent，交付后final并结束。
