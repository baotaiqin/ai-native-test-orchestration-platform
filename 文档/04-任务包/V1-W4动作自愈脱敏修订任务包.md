# V1-W4/r4 动作失败自愈脱敏

已派发至既有 AI-Test-Runner_sol，gpt-5.6-sol / xhigh。r3 轮次 completed/idle 后主控集中下发。先读根 AGENTS.md；禁止跨任务消息或等待。保留 r1/r2/r3 的证据，不得覆盖历史失败。

## 复现与目标

r3 最终 executor 指纹为 06c231ad91f4d99d8074699276b4eab67620246a50a6568912d8020487b75851。主控原 9 项真实 Chrome 通过（19.65 秒），但新增第 10 项确认另一实际入口仍泄漏：计划含随机标题 ASSERT_TITLE expected，之前 CLICK 的全部 Locator 未命中；`_perform_locator_action` 调用 `_collect_healing_context(page, locator_plan)` 未传入计划已解析的禁止值，最终 action_1 的 healing_context.page_title 和 result.to_wire 含该值。主控测试 `.codex-validation/test_v1_w4_controller.py::test_planned_title_is_private_when_action_fails_before_assertions` 实际失败 2.83 秒。早期测试缺 failure_policy 的协议拒绝仅为夹具纠正，不是产品缺陷。

将同一执行范围内的已解析计划禁止值及适用已观察值覆盖动作和断言全部自愈采集入口，避免入口各自遗漏。保留安全候选、锁定版本、错误类别、节点归属、W1 动作与 r2/r3 的截断/上限处理；不以全局禁用 healing 通过测试。检查全部调用点，覆盖动作 STOP/CONTINUE 及计划 FILL/SELECT 与未执行断言中的已解析值；测试随机标记只输出泄漏布尔值，不输出原值。

## 范围与交付

只修改 `runner/runner/executors/web.py`、`runner/tests/test_v1_web_assertions.py` 与必要相关 Runner 测试；protocol/models 冻结，不改 Backend/Frontend/部署/正式服务。先将当前 `.codex-validation/v1-w4/result.json` 原样归档为 result-r3.json，再更新独占 result.json 和 `文档/05-交付记录/V1-W4基础Web断言Runner交付.md`。禁止修改主控测试及全局文档。

真实 Chrome 主控 10 项、W4 专项、W1 相关动作回归、Runner 全量及 Ruff 通过，交付最终指纹与清理结果。只允许测试自有回环页面/进程/临时证据；不调用 AI、正式数据库或 Git/Claude/内部 Agent。完成或超出范围时写报告并结束，主控主动读取。
