# P6-I1M/r1 需求关联真实 MySQL 验收任务包

主控已审查 P6-I1/r2，Backend446项全量、主控18项独立检查通过，14项源码指纹一致；本包派发给 AI-Test-DevOps_sol，模型 gpt-5.6-sol，xhigh。冻结依据 `.codex-validation/p6-i1-controller-review.json` 的14项指纹和r2最终交付；执行前核对，不自行接受漂移。0039迁移SHA256为 `857ab58f765246203c6f72eef7f99e55b76c8d3e15c598b64c83470a2017b316`。只有主控的实际派发消息构成启动指令。

## 目标与范围

在独立临时 MySQL 8.4 环境验证 0038→0039 需求关联迁移、非空旧数据兼容、新外键/唯一性/状态约束及实际关联服务并发行为。不能仅以 SQLite、生成 SQL 或静态检查替代 MySQL 结果。

独占脚本与证据 `.codex-validation/p6-i1m/`，独占报告 `文档/06-验收记录/P6-I1M需求关联MySQL验收.md`。允许创建有明确本轮标签、随机回环端口的临时容器及其专用卷；创建时记录所属容器/卷/网络，退出时逐项清理并验证。复用现有已下载镜像，临时数据库凭据只用于该环境，不写入日志或交付。允许启动仅连接此临时实例的短期验证进程。

不修改产品源码、迁移、Backend 测试、其他任务证据或全局文档；不读取正式凭据，不访问正式业务数据库、不重启正式服务、不连接共享 Demo、不调用 AI、不创建正式资产或 Run。检查失败时保存完整失败点与可重现证据，清理所属资源后结束，由主控决定返工，不发消息求援或等待其他任务。

## 必须验证

1. 空库从 base 升至最终 head，结构反射核对约束/默认值/可空性/外键目标及索引；0039 空表降至0038再升至0039。真实执行能够识别索引与 FK 删除顺序错误，不能通过禁用外键检查绕过。
2. 在0038创建合成项目、需求/版本、原 API TestCase 和旧 WEB TestCase、旧关联。保存关联所有既有字段和相关版本内容哈希。非空升级后原 ID/source/confidence/relation_type/创建审计完全保留，资产种类准确，未记录的版本保持未知，不按 current 或时间补写。保留独立 WebCase 相同数字 ID 的对照，不能误连接。
3. 对仅包含旧兼容数据的0039降至0038并重升，数据仍等价。分别含新精确版本、独立 WebCase、移除历史/再关联审计的数据，downgrade 应在任何破坏性 DDL 前明确拒绝，拒绝后 schema 和数据不变；不删新记录换取降级成功。
4. 实际 MySQL FK 拒绝不存在的资产/版本/前驱链接；目标 CHECK 拒绝双目标、空目标和类型错配。状态 CHECK 必须拒绝 ACTIVE+NULL slot、ACTIVE+错误 slot、REMOVED+活动 slot、REMOVED+缺审计。直接 SQL 不可利用 NULL 三值逻辑绕过活动唯一约束。
5. 新关联创建、移除、再次创建保存新 ID 和 supersedes，历史版本/审计不覆写；相同需求、资产、relation_type 只有一条 ACTIVE，同目标允许保留多条 REMOVED。项目/资产/版本归属由真实服务拒绝，不把“外键存在”误报为“归属已验证”。
6. 使用两个独立真实 MySQL 事务做有界并发创建及移除/再关联，记录成功/冲突结果和最终数据，确认无重复 ACTIVE、无半提交，冲突不遗留失效事务。设置合理锁等待超时，失败不无限重试。
7. 验证新时间写入与 HTTP/服务响应符合最终 UTC 契约；如存在旧时间来源标记，旧未知保持未知。切换临时连接的 session time_zone 后，不能让新的关联审计依赖服务器本地时区。包括显式关联、手工创建附带关联及已有合成 AI 建议接受（无模型调用）的路径。

## 交付

报告列出版本、镜像 digest、冻结源码指纹、准确命令与逐场景结果、失败证据及清理结果。保存机器可读 JSON 和有界日志；只记录合成值，不输出口令。主控拉取报告和完成状态后复核，不能主动调用跨任务工具。遵守根 AGENTS.md 的主控独占调度，不使用 Git、Claude 或内部 Agent。
