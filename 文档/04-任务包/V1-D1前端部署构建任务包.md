# V1-D1/r1 前端服务器镜像与 nginx 配置

主控派发AI-Test-DevOps_sol，Sol/xhigh。先实现可交付的前端部署构建基础，不等认证/Secret Provider，不启动服务。完整服务器栈由后续Backend镜像/Compose包接入。

新增deploy/server/frontend.Dockerfile：项目根构建上下文，读取frontend/package-lock.json用npm ci，多阶段构建Vite产物，nginx非root运行（监听8080），只复制必要源码/静态产物，不COPY整个仓库，不把.env、.codex-validation、个人配置/证据打入镜像。提供Dockerfile专属ignore（路径遵循Docker实际支持形式），避免改全局忽略和前端包。现有源码将由Frontend W9更新，禁止修改它；不因构建产物变化反复运行npm。

新增deploy/server/nginx模板或配置和最小启动脚本：SPA fallback、assets静态缓存、index不长期缓存；/api/v1代理稳定内部backend:8000，并保留原URL前缀，不把/api错误退回SPA。SSE不buffer、不缓存，合理读超时，保留流式连接；请求体大小与后端既有上传限制核对。提供无凭据静态健康路径。nginx可写pid/cache/temp均位于明确可写路径，非root不依赖写/etc权限。模板需要环境替换时只允许明确变量，不误替换nginx $变量。不得配置任意用户给定upstream URL，默认服务DNS backend。

文档/01-使用指南/服务器前端部署说明.md给出明确项目根docker build命令、内部网络/后端服务名端口、TLS终止与反代边界、Windows Runner独立说明和当前未验项；不能声称完整V1部署完成，不包含真实密码或手工逐步提示用户现在执行。

范围仅deploy/server/本包新前端Docker/nginx/启动/ignore文件、上述独占说明、.codex-validation/v1-d1/、文档/05-交付记录/V1-D1前端部署构建交付.md。不改Backend/Frontend源码、认证/Secret Provider/现有dev Compose、正式服务、旧验证证据或主控文档。

基本检查：Dockerfile/脚本/配置静态一致性和已有工具的解析，若Docker可用且无需拉取可选择一次隔离配置检查；引擎不可用或需网络镜像不视为开发阻塞，不启动Docker、不提权安装、不实际部署。镜像构建、nginx现场、SSE/上传/重启留集中验收；清晰记录未验。完成简短报告并结束，不跨任务消息/等待、Git、Claude或内部Agent。