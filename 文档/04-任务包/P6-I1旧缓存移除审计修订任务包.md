# P6-I1/r4 旧缓存移除审计修订

I1/r3已自行completed/idle，未人为中断。主控核对最终15项指纹全部一致，最终service与23项独立检查时相同，确认22通过1失败；主控全量456项和Ruff通过，现向空闲Backend派发本包。gpt-5.6-sol / xhigh。

## 唯一修订目标

主控独立 `.codex-validation/test_requirement_link_identity_review.py` 复现：Session A先缓存ACTIVE关联；独立HTTP请求完成移除；A调用实际remove_requirement_link时，带FOR UPDATE的查询返回数据库行，却沿用identity map中旧ACTIVE属性，因此重复移除被接受并覆盖原removed_at/removed_by。数据库锁与ORM对象属性刷新是两个问题。

r3工作中service指纹65283038885d5a0c0aa2692dcf7aef41df2d41ce652aa5889d7a97ea7d92533f上，主控23项为22通过1失败（4.64s）。此前4项1205/1213 HTTP故障注入已通过，原迁移/UTC等18项也通过；只剩上述旧缓存移除历史保护。最终是否仍需本包由主控交付后复验判断。

修复当前读时的ORM旧缓存，重复移除应受控拒绝且原移除响应/数据库审计完全不变。保留r3需求行先锁、前驱当前读、事务锁错误处理和提交后响应读取边界，不使用全局expire_all、移除锁、改隔离级别或清空业务历史来掩盖问题。当前读必须确实依据数据库状态决策。

## 范围和交付

仅允许requirement_links/service.py局部修订与关联专项测试，保存r3机器结果副本，更新p6-i1/result.json及原独占后端交付报告。不得改API Schema、迁移、其他模块/任务文件、主控探针、正式环境或全局文档。

运行主控原22项加identity_review共23项、对应后端专项和Backend全量/Ruff。新增回归应保留强引用的旧缓存对象并走实际服务/HTTP，不是只断言SQL含populate_existing。真实RR/RC仍由后续I1M/r2完成，本包不创建MySQL或等待DevOps。

先读根AGENTS，禁止跨任务工具、Git、Claude、内部Agent。完成后final并结束，由主控拉取；不自行开始W2或其他工作。
