# V1-W4/r3 脱敏集合数量上限修订

已撤销的预审草稿，从未派发。r2 执行者在交付前读取主控新增探针并修复，主控原 8 项最终全部通过；数量溢出不再需要此修订。实际 r3 另见《V1-W4自愈上下文脱敏修订任务包》，不得执行本文件。

## 问题与验收事实

主控 `test_v1_w4_controller.py::test_observed_value_count_limit_keeps_final_title_private` 使用合法的 51 个断言、真实协议解析与 Chrome。前 50 个 INPUT_VALUE 各通过两候选回退观察两个不同值，全部断言成功；第 51 个 TITLE 读取实际标题并按错误 expected 失败。共观察 101 个值，达到集合 100 条上限后，最后的标题不在 forbidden 中，仍进入 WEB_SUMMARY wire metadata 和 JSON。不是构造超过协议 100 断言的非法计划，也不是仅手工塞满私有集合。

预审证据 `.codex-validation/v1-w4-controller-r2-preaudit.json`，当时 1 failed / 7 deselected，6.26s；最终结果由主控派发消息补充。r2 对单个长值的修复保留，不能只增加本次计数刚好通过的常量。

## 目标与范围

使数量/长度上限触发时的所有相关证据字段同样安全。既然已经无法保留完整观察值集合，就不能仅丢弃 Trace/截图，却继续把可能包含遗漏值的标题、Console 或 Network message 原样写入 wire/JSON；正常 finish 和 abort 必须采用一致的有界安全策略。

可在明确的溢出情形省略不再能可靠脱敏的文本，保留正常未溢出情形的公共信息与结构。不能无限增加暂存内存，不能全局清空正常证据或改协议来拒绝合法计划。数量上限应考虑候选回退，一个断言可能读取多于一个实际值。复用既有异常收尾，保持文件字节与 metadata、size、sha256 一致。

仅改 `runner/runner/executors/web.py` 和相关 Runner 专项测试；protocol.py/models.py 正被 Backend W2 只读使用，必须保持冻结指纹，不改 Backend、Frontend、部署、队列、全局文档或主控测试。保留 r2 result 为 result-r2.json，独占原 `.codex-validation/v1-w4/` 与原 W4 交付报告。

## 验证与结束

主控 W4 当前 8 项必须全部通过，W1 主控 5 项及原 W4 专项、Runner 全量/Ruff通过；补少量 normal/abort 的溢出安全验证即可。记录准确命令/次数/全量中的机器负载计时波动，不能删失败记录或改变 W1 测试护栏换取通过。全部使用独立随机回环、所属 Chrome/profile，不访问或重启正式服务。

先读根 AGENTS.md；禁止跨任务消息、等待其他任务、Git、Claude、内部 Agent。完成报告与机器证据后结束，由主控读取。本草稿不是执行授权。
