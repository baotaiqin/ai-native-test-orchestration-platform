# V1-W4/r3 已观察值进入后续自愈结果的修订

主控现派发给 AI-Test-Runner_sol，gpt-5.6-sol / xhigh。r2 已 completed/idle，结束游标 :13。r2 的长值与数量溢出修复已由主控真实 Chrome 原 8 项全部通过（19.24s）；本包针对最后审查新增的独立跨节点结果泄漏。不是重新处理已经修好的数量溢出草稿。

## 最终源码复现

r2 executor SHA256：1033a07c0d27024736edd8e81ef7f174809a567b3615ee56b3614862a9cec7b8。

主控 `.codex-validation/test_v1_w4_controller.py::test_observed_title_does_not_reappear_in_later_healing_result` 在真实 Chrome 页面先执行 ASSERT_TITLE（已知 expected 等于随机实际标题，成功），再执行绑定 element_version_id=42 的 ASSERT_EXISTS（不存在的元素，产生受控 ASSERTION_FAILED 及 healing_context）。后一 trace 的 healing_context.page_title 将刚刚观察过的标题写入实际 result.to_wire。

主控新增第 9 项最终结果为 1 failed / 8 deselected，2.55s。首次探针误将此断言错误期望为 WEB_LOCATOR_NOT_FOUND，已按照实际断言契约修正为 ASSERTION_FAILED；修正后失败明确发生在真实 wire 泄漏断言。不是 repr 检查、预期错误码断言或虚构结果。

r2 对 Evidence 文件和 manifest 的脱敏集合没有覆盖执行结果中的 healing_context；正常执行出口直接携带这些 traces。必须保护整个结果传递路径，不能把只修证据文件视为所有可见结果都已安全。

## 目标与范围

已解析的 expected、已观察到的实际 title/text/input 值在后续 locator 失败时，不能通过自愈上下文的标题或候选文本、执行 trace、结果 wire 或 Evidence 重新暴露。保留有用且安全的定位线索、原 element_version_id、节点归属、候选状态与业务错误类别，不直接全局关闭自愈功能。

检查值收集、上下文采集/截断与结果序列化的先后关系；不能在候选或标题先被截断后，仅用完整字符串匹配而漏掉前缀。无法可靠脱敏的局部字段可安全省略，正常字段保留；沿 r2 有界内存与溢出安全策略。不新增断言能力、重试、动作重放、模型调用或生产网络访问。

仅允许 `runner/runner/executors/web.py`、必要的直接脱敏 helper 和相关 Runner 专项测试。protocol.py 与 models.py 正被 Backend W2 只读使用，必须保持 dd9f92c0aa7bb63c3682a9eb5d39a25f574128522b4843186fbba464d7ebad83 / f33405453276d19bac96748bbf69aa93df55bfcc48b75e40a2dbd67c0880a16a，不得改 Backend、Frontend、队列、迁移、部署或全局文档。

保留 `.codex-validation/v1-w4/result.json` 为 result-r2.json，再输出 r3；独占原 v1-w4 目录和《V1-W4基础Web断言Runner交付》，不得修改主控探针/证据或旧 W1 证据。

## 验收

主控 W4 当前 9 项全部通过；补有限个跨节点、标题/候选字段与截断边界的真实结果验证，同时确认安全候选仍可用于自愈。测试真实执行出口和 to_wire，不只 mock 内部字段。回归 W1 主控 5 项、W4 专项、Runner 全量及 Ruff。计时波动如实记录，不修改 W1 测试来掩盖失败。

先读根 AGENTS.md。禁止跨任务消息/等待、Git、Claude、内部 Agent。只使用所属随机回环服务和 Chrome/profile，精确清理；不访问或重启正式环境。完成结果、报告和指纹后结束，由主控读取；超界保存阻塞和检查点后结束。
