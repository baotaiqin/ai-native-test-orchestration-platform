# P6-I1M/r2 需求关联并发复验

Backend I1/r4已completed/idle并通过主控审查：457全量/Ruff、主控23项、最终15项指纹通过。DevOps上轮已completed/idle，主控现派发本包（gpt-5.6-sol / xhigh）。冻结依据 `.codex-validation/p6-i1-controller-r4-review.json` 的hashes数组，service为e470987e2b8a2940fb0fa58e1a75d2680740a9b739d24da03d5e8b0849caf176。r4补充旧缓存对象的移除审计保护，r3的事务修复保持。

## r1 审查结论

保留 r1 的真实0039迁移往返、17项约束拒绝与UTC验证成果。整体并发门禁未通过：验证器指定READ COMMITTED；原移除提交前、flush前合法交错产生数据库异常或双方都无成功，最终改成after_flush后通过。主控故障注入发现产品公开HTTP对1205/1213返回500。不可将未被产品处理的DBAPIError标成可接受冲突，不能删除失败时序换取通过。

r1结果SHA256为 cbcc73caec72eefc48469fe987f640422f556aafcbd28c7dc48225fb813e5c1f，原脚本3b99617d0c9fb8976ee0f7073ccb67abe18a290b35f241ec0211fcd07a6844f2，attempt-log为39965d3e6088afbe37a1b59fc73d4f5753fc7581ec89bd6169f4dcdc3124d594。原文件与报告保留为历史，不覆盖。

## 范围

本轮只写 `.codex-validation/p6-i1m-r2/` 和 `文档/06-验收记录/P6-I1M并发复验交付.md`。可复用r1验证器逻辑但在新目录实现、保留每次结果。仅创建带 `com.openai.codex.package=P6-I1M-r2` 及随机run标签的独立MySQL8.4容器、专属卷/网络和随机回环端口，记录并清理精确所属资源。复用已下载镜像，秘密仅临时环境/内存，不写结果或日志。

不改产品、迁移、Backend测试、主控探针或全局文档；不访问正式库/凭据/共享Demo、AI、正式Run或重启服务。执行前后核对派发消息指定的新冻结指纹；不自行更新冻结值接受漂移。正式ready/owned文件只核对指纹。

## 必须验证

1. 实测未指定隔离级别的新引擎 `@@transaction_isolation`；默认REPEATABLE-READ为必验，另独立READ-COMMITTED。不能只设置RC或更改全局参数。记录每个参与连接的实际级别与连接ID，禁止共用一个Session冒充并发。
2. 两个真实产品服务并发创建同一关联：恰一成功、一受控业务冲突，无重复ACTIVE、无半提交，冲突事务由产品处理后可继续使用。测试程序不能先rollback再声称产品恢复。任意未处理DBAPIError均失败，需保留类型/安全错误码/阶段。
3. 两侧真实产品服务并发移除和再关联，分别覆盖原before_commit/flush前暂停、after_flush/commit前暂停。确认真实锁等待后释放，不能用原始SQL替代一侧、只改同步点或隐式retry。允许明确串行顺序产生受控已存在冲突，但必须解释提交顺序；移除提交在先并允许新创建时，结果应为新ID指向刚移除前驱，旧记录不改写。若需用户显式重试作为单独场景，记录初次结果与retry_count，不能吞初次失败。
4. 在创建Session先打开旧RR读视图（读需求/旧关联），另一Session完成移除后，原Session调用创建，仍精确捕获最新REMOVED前驱。另覆盖原Session已缓存旧ACTIVE对象的情况，确保当前读刷新ORM状态；在RC作对照。历史链接/版本/审计指纹保持，1ACTIVE和预期supersedes链。
5. 使用实际MySQL独立事务持锁制造有界1205，公开HTTP创建和移除返回约定409/503、安全错误结构，数据库无意外写入，释放后新请求可明确成功。验证READ/flush/commit覆盖证据来自真实产品路径；1213若实际死锁不可稳定制造，必须如实列为故障注入补充与已避免的时序，不冒充真实死锁。可以保留独立故障注入验证，但不可替代1205实测。
6. 锁等待、同步屏障、线程future、进程均须有界；超时保存阶段与结果后清理，禁止无限循环。失败时先区分真实产品错误和验证器错误；只有有证据的验证器缺陷可修复有限重试，原始尝试结果永久保留。不得把任意OperationalError统一当作预期冲突。
7. 0039迁移本轮未变，可复用r1详细迁移证据并核对哈希，无需重复每项升级降级。新临时库仍需真实Alembic升级与合成数据；变更关联服务的历史/版本归属/UTC冒烟至少覆盖显式创建、移除、再创建。

## 交付

结果每次独立命名并有总索引，记录版本/digest、冻结指纹、实际隔离级别、每个场景同步点/成功冲突与错误码、是否有重试、事务恢复由谁执行、最终链与历史指纹、资源前后清单及清理。报告明确真实MySQL、HTTP、故障注入三类证据，不将未验分支计为通过。完成或阻塞后final并结束；禁止跨任务消息、等待、Git、Claude、内部Agent，由主控读取。
