# V1-W4/r2 证据截断与脱敏修订

派发给已有 AI-Test-Runner_sol，gpt-5.6-sol / xhigh。r1 已 completed/idle，主控已读交付并对最终源码独立复验；本包集中处理验收缺口。先读取根 AGENTS.md，禁止跨任务消息、等待、Git、Claude、内部 Agent。只在本任务内工作并交付后结束。

## 已确认问题

r1 executor SHA256 为 afaa796767665b8cfc373e7242dfaa545cca69871f305aa5cf8c6c1a791fa088。主控 `.codex-validation/test_v1_w4_controller.py` 最终源码真实 Chrome 7 项：5 passed、2 failed，15.26s。两个失败分别为 ASSERT_TITLE 和 ASSERT_INPUT_VALUE；随机长实际值与错误 expected 不同，真实 manifest.to_wire 和 JSON 文件均泄漏实际值的截断前缀。标题在 web-summary.json，输入值在 console-error-1.json。测试没有打印敏感值，也没有用隐藏 metadata 的 repr 代替实际 wire 检查。

原因是已知敏感实际值在收尾才合入 forbidden，但标题或 Console 已先截断到 256 字符；后续完整字符串脱敏无法匹配已截断的前缀。r1 的二次 redact 不能修复这个时序。该问题是独立本机合成 Chrome 复现，不涉及正式用户数据。

## 修改范围与要求

仅允许 runner/runner/executors/web.py 与 runner/tests 中相关证据/断言专项测试；确有必要才局部修改直接使用的脱敏辅助函数，并在报告说明。协议及 models 保持 r1 指纹，不新增动作/断言/载荷，不改变旧 ASSERT_TEXT 包含语义或 Network 既有 message 字段。

对已纳入隐私契约的 expected/observed 值，在不可逆截断前正确脱敏；如值在采集后才被观察到，使用有明确大小/数量上限的暂存或安全省略策略。检查正常 finish 及 abort/fallback 路径，不把已知原值通过错误回退重新写入。不能依赖无限内存暂存、全局清空所有公共 metadata 或只修测试字符串。最终文件字节、sha256/size 和 wire metadata 必须相互一致。

保留 r1 result.json 为 result-r1.json 后再交付 r2；原 W1 证据及主控探针/证据不得修改。独占 `.codex-validation/v1-w4/` 和 `文档/05-交付记录/V1-W4基础Web断言Runner交付.md`。不修改 Backend/Frontend、迁移、队列或全局文档，不重启或访问正式服务、注册 Runner、创建正式 Run、调用 AI。只用所属随机回环服务、Chrome/profile，并精确清理所属资源。

## 验收

使用正确 PYTHONPATH 或 runner 工作目录执行主控 W4 7 项、W1 5 项，均须通过。补足有限个针对实际 wire、文件字节、正常/异常收尾、长值与晚观察时序的回归；不以 repr 或 mock 的 PASS 替代。运行 W4 专项及 Runner 全量/Ruff；必要命令失败如实保留，最终报告记录准确数量、耗时、指纹、资源清理和仍存在的边界。交付后停止，由主控统一读取与验收。
