# V1-W9/r1 隐藏与可点击断言前端接入

主控已派发给AI-Test-Frontend_sol，gpt-5.6-sol/xhigh。W8后端已实现、基本检查通过；W7 Runner已交付。按最新规则先开发，集中验收后置。

在现有WebCase手工编辑与录制AI建议人工编辑接入ASSERT_HIDDEN和ASSERT_CLICKABLE。资产DSL严格为type/locator/timeout_ms，locator必填、timeout整数100..600000，不含expected。隐藏含不存在或不可见，可点击是trial检查而非真正点击。中文标签准确区分旧VISIBLE、EXISTS、ENABLED。类型切换使用现有统一工厂清除无关字段，不修改原8断言语义；保留20动作、版本、元素引用、Session、身份/权限和AI已编辑接受路径。

独占允许frontend/src/types/web.ts、frontend/src/utils/web-assertions.ts、WebAssetView.vue及WebRecordingWorkbench.vue必要局部、对应小型工厂契约测试；不得改报告I2F、认证、Backend、Runner、全局文档。优先扩展已有共享工厂，避免双份逻辑。独占证据.codex-validation/v1-w9/和报告文档/05-交付记录/V1-W9隐藏与可点击断言前端交付.md。

基本检查只做类型检查、一次构建和真实工厂输出对当前Backend schema的针对性校验（合法、缺locator、类型切换无expected）。Backend A1正在改认证，不启动Backend应用或正式接口；只读web_cases/schema安全。无需Chrome矩阵、截图、W3/W6/I1F重复回归。最终浏览器编辑/权限/AI完整链路列待验即可。

完成后简短交付修改文件、基本检查、未验事项及阻塞并结束。禁止跨任务消息、反向等待、Git、Claude、内部Agent和正式服务。