# P6-I2/r2 关联缓存与单语句快照一致性

已派发至既有 AI-Test-Backend_sol，gpt-5.6-sol / xhigh。r1 已 completed/idle，主控在最终审查后集中派发。先读根 AGENTS.md；禁止运行中跨任务消息、等待或扩展范围。

## 唯一修订点

r1 已把 Requirement/RequirementVersion 展示字段改为联表标量列，旧需求对象缓存问题与旧关联时间误加 UTC 的问题均已修复。但查询仍选择整个 RequirementCaseLink ORM 对象，后续读取 `link.requirement_version_id`、资产版本、关系、来源、置信度与审计字段仍可能取到旧 identity map 值，不能保证这些字段都来自该条 SQL 的视图。

主控 `.codex-validation/test_p6_i2_controller.py` 现有4项：缓存项分别保持 Requirement 与 RequirementCaseLink 强引用；另一个 Session 原子更新需求标题和该 Link 的需求版本，原 Session 新事务捕获。Requirement 项通过，Link 项最终返回 requirement_version_id=None，而 SQL 数据已是103。此为隔离 SQLAlchemy 低层变更场景，不声称生产 API 会直接修改不可变 Link 版本；本修订要求严格兑现单语句数据库视图契约，消除对 Session 缓存历史的依赖。

最终r1主控组合：上述4项加 W2/W5 各1项，5 passed / 1 failed，6.31秒。源码 `run_requirement_snapshots/service.py` 指纹为 a380c79cc1daf67ac6ba6ba1ab7e82dac866ee11ad28b8f12c0354e14b4a6c4d。唯一失败为 `[RequirementCaseLink]` 参数分支。

将所有要捕获的 Link 字段也从同一联表语句的结果取得，或采用同样明确可靠的刷新方式。优先标量列，保持单次有界读取和原 RR/RC 语义；不要引入逐行查询、全局expire、任意事务提交或不同视图拼接。检查最终查询中所有 ORM 实体是否仍可携带旧缓存，保证字段来源完整。

## 范围与验收

只允许 `backend/app/modules/run_requirement_snapshots/service.py`、必要的 `backend/tests/test_run_requirement_snapshots.py`、独占 `.codex-validation/p6-i2/` 与 `文档/05-交付记录/P6-I2运行需求快照后端交付.md`。先把r1 result.json原样归档为 result-r1.json，再写r2最终证据。模型、迁移0040、报告协议、Runner、Frontend、正式服务及全局文档不修改；如确有超范围依赖，写阻塞报告后结束。

保留新/旧/空/不适用状态、未知绑定、1000条上限、原子回滚、报告分页/完整导出、可靠UTC与LEGACY_UNKNOWN原时间语义。主控4项与W2/W5、相关专项、Backend全量和Ruff通过，最终指纹与结果对应；测试脚本只读运行，不能修改主控探针。检查未变代码可复用既有证据，产品未变化时不因单纯编写报告反复跑全量。

不访问正式数据库、Broker、MinIO、AI或凭据，不启动/迁移/重启服务，不使用Git/Claude/内部Agent。完成后交付并结束，由主控读取；真实MySQL门禁仍待后续I2M。
