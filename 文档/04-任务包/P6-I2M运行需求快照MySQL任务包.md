# P6-I2M/r1 运行需求快照真实 MySQL 门禁

Backend I2/r2已completed并通过主控源码/HTTP审查：16最终指纹、6项主控组合5.84秒、624全量及8专项通过。25项输入已冻结；现派发至无活动轮次的AI-Test-DevOps_sol（gpt-5.6-sol / xhigh），以本次主控消息启动。

## 前置与独占

按 I2 最终模型与迁移补齐精确文件/指纹清单，不对正在修改的代码验收。只允许独占 `.codex-validation/p6-i2m/`、报告 `文档/05-交付记录/P6-I2M运行需求快照MySQL交付.md`，不得修改产品/迁移/其他证据。先读根 AGENTS.md；禁止任务间消息和等待，不连接正式服务或读取正式凭据。

使用测试自建 MySQL 8.4 实例与唯一随机资源名、随机凭据；Docker 资源使用明确包标签。脚本 try/finally 精确清理本次实例、网络、卷及凭据；不复用其他包或用户资源。原始失败尝试单独保留，不在相同记录覆盖成成功。全程不得输出密码、DSN 或种子中的合成敏感标记原值。

## 真实结构与旧历史

在真实 Alembic 0039 建立旧 Run/CaseRun、既有需求关联和不可变版本合成数据；保存摘要、主键及关系，再升级 I2 的下一迁移。旧 Run 仍无捕获且读取为 NOT_RECORDED，不根据当前关联回填。

空新增表允许降级/再升级时核对 FK/索引与旧数据保持；非空捕获表必须在任何破坏性 DDL 前拒绝降级。检查捕获头、条目、旧表、行数与数据摘要全部不变，随后服务仍能正常读取。真实检查 MySQL 外键名、nullable CHECK、唯一头、条目重复，以及头→CaseRun、条目→头的孤儿写入拒绝；不能用 SQLite 成功替代。

I2 目前采用独立不可变来源副本，源需求/版本/关联身份没有指回实时源表的外键，以避免源表删除擦除历史。主控允许这项局部设计；验证创建时的类型/版本/项目归属校验和源表变化后快照保持，不把“没有源表 FK”本身误判为失败。头/条目与所属 CaseRun 的真实外键仍须有效；最终结构以主控冻结结果为准。

## 实际服务链

使用真实 MySQL 的应用 Session，身份、Broker、Redis、对象存储仅使用明确的受控替身。走实际 HTTP 创建 Run、读取 CaseRun/Report、分页、两种导出和必要 Dispatch/Claim。禁止真正向队列投递或启动浏览器执行。

覆盖 API_CASE 与独立 WEB_CASE、同数字 ID 的两类资产、精确执行版本匹配、其他精确版本排除、旧资产级关联明确未知、新无来源 CAPTURED_EMPTY、Scenario 不适用。修改当前需求标题/current、移除/重新关联、归档来源后，旧 Run 的持久来源及导出保持原事实。新 Run 显示新捕获，不能把当前事实写入旧 Run。可靠捕获时间输出 UTC；LEGACY_UNKNOWN 的原关联时间保留未知口径，不为其补造UTC偏移。

创建异常/超限时 Run、CaseRun、StepRun、捕获头/条目全部回滚，不出现 Outbox 或事件。使用1001条测试来源证明拒绝而非静默截断；1000条成功场景检查分页与完整导出。若导出超出原16MiB上限，必须受控拒绝而非返回缺项文件。

## 并发一致性

记录实际服务器默认隔离级别，默认 RR 与显式 RC 分别使用独立连接和真实事务，记录 connection_id。对“捕获来源读取”与“移除/重关联/需求新版本和标题更新”制造可控时序，明确每次提交和读取边界。

若实现以单条 joined SELECT 捕获，验证结果对应该语句的一致视图；若使用明确锁定方案，验证锁等待/提交后的预期结果。不得只断言无异常。检查同一条目中的 Link/需求版本/资产版本/标题/hash都是一个可解释数据库时点的组合，不能各自来自变化前后。测试先建立旧 RR 视图、再另一连接提交修改的场景，并按产品真实捕获契约评价，不能假定 RC 与 RR 应返回相同数据。

对重复提交、锁冲突、失败后 Session 与新请求恢复如实记录。只有实际造成的 MySQL 错误才标“真实”，故障注入与未复现的1213单独列出，不用替身冒充真实死锁。缺陷保存可复现证据后结束，由主控决定 Backend 修订；不得自行修产品。

## 交付

记录冻结源码前后指纹、每次命令退出码、原始尝试索引、场景断言及资源清理结果；复核正式 ready/owned 与 P5-E 保护历史摘要不变。所有场景完成后结束本轮，由主控读取并独立审查。最终源码和完整 MySQL 门禁通过前，不授权正式迁移/加载。

## 本次精确冻结与并行隔离

唯一输入清单 .codex-validation/p6-i2m-frozen-inputs.json，含25项Backend源码/迁移/测试SHA256；上游审查 .codex-validation/p6-i2-controller-r2-review.json。运行前后逐项校验。0040已冻结，正式库仍0038。Runner正在自己的W7断言包，Frontend正在W6；本包使用Backend实际Dispatch/Claim返回与固定协议事实即可，不导入正在变化的Runner执行器，也不等待或联系它们。Backend本轮保持空闲与源码冻结。
