# V1-W7/r2 Playwright依赖下限

r1因额度限制failed，已无活动轮次。主控只读核查无相关验证遗留进程，5项源码指纹与r1 result一致，主控3项9.52秒通过；471全量+2skip与10专项/Ruff已落盘。现派发r2，同时完成中断时尚未写出的独占报告，不将failed冒充完整交付。

主控查到ASSERT_HIDDEN调用Locator.filter(visible=True)，官方文档明确visible选项自v1.51加入，但runner/pyproject.toml允许playwright>=1.50,<2。当前本机新版本Chrome测试通过，不代表声明的1.50兼容。

依据：[Playwright Python Locator.filter visible](https://playwright.dev/python/docs/api/class-locator#locator-filter-option-visible)。这是依赖元数据与实际API要求不一致，无需修改隐藏语义或为兼容旧版放宽断言。

主控授权本修订额外修改runner/pyproject.toml，将最低版本与已使用API对齐（至少1.51，保持原主版本上限）；若仓库实际有同一Runner依赖清单/锁文件，必须一致，先明确列出而非全仓依赖升级。不修改当前虚拟环境、浏览器安装、其他包依赖或正式Worker。若依赖由其他权威文件固定在冲突版本，报告该具体依赖后结束。

先原样保留r1 result.json为result-r1.json，更新本包专属交付和机器证据。产品执行器/协议若与r1最终指纹不变，可复用r1真实Chrome/全量证据，不因为只改依赖下限而重复全量。用实际packaging元数据或标准SpecifierSet证明1.50被拒绝、1.51与已测试安装版本被允许，确认项目依赖格式有效，记录最终全部相关指纹。完成final后结束，由主控审查；继续禁止跨任务工具、Git、Claude、内部Agent和正式服务。

恢复基线：r1 result.json SHA256 9ce4359a97f15c2070996114393ac467aace32e1bf7ce3bd0a9ba922507f4e1b；protocol为1ba7f9a84bb21885a4fe7de2bafd875757d93170099394386836292268e67e6e，executor为15eaa09deb6e35e3e7abfab8d19bb2427d0410294526a73981c1d1d2afd2db7b。主控恢复检查点见 .codex-validation/20260910-worker-recovery-review.json。完成r1缺失的最终报告并在r2明确复用证据范围，不因补文档重跑全量。
