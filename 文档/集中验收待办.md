# V1 集中验收待办

> 2026-09-13 更新。完整 V1 主流程实现为 100%，独立 MySQL、真实 Runner/Python HTTP/SSE/JMeter/Scenario、Prometheus 故障恢复以及全量代码门禁均已完成。以下清单记录暂时无法提供环境的 V1 发布前待验项，仅保留需要真实浏览器、真模型、公网 CI/Webhook 或 Linux/服务器环境的发布门禁；完成前不声明完整发布验收通过。

## 2026-09-13 全面验收及阻断修复结论

- 初次验收与发布续验累计发现的 6 个代码阻断已全部修复，完整证据见《06-验收记录/V1全面发布验收记录》同日续验章节。
- 已解除 1：报告导出测试已同步“请求配置快照”；Backend 全量复验 836 passed / 1 skipped / 0 failed。
- 已解除 2：登录页不再受工作台 1180px 最小宽度影响，并新增 900/480px 单列断点；Frontend 类型检查和隔离 production build 通过。computer-use 连接失败，真实窄屏截图仍在下方浏览器门禁中保留。
- 已解除 3：补齐真实 Schema 声明，并让 Alembic 以表达式语义比较 MySQL CHECK、精确豁免 3 个仅由迁移维护的循环外键；`alembic check` 无待升级操作，未改动本地数据库。
- 新增通过：独立 MySQL 空库与 `0076↔0077`/head 往返、真实 MySQL 认证 100 次稳定性、真实性能错误矩阵/Warm Up/SSE/SLA/历史/对比、JMeter、Scenario 性能、Prometheus `up→down→up` 和容器重启数据持久化。
- 最终全量：Backend 843 passed / 1 skipped，Runner 575 passed / 2 skipped，双方 Ruff、Frontend type-check 与 1835 模块 production build 通过。
- 未完成：认证后浏览器及缩放/拖拽、真模型 AI Performance Analysis、公网 HTTPS Webhook/真实 CI、Linux 资源指标与服务器 Compose/双实例门禁。

## 项目模型批量绑定待集中验收

- 在项目设置“模型绑定”选择一个启用模型并点击“设为所有任务的主模型”，确认全部任务一次更新，原本与新主模型相同或类型不兼容的备用配置被清除；重复操作应全部显示为无需修改。
- 先只为部分任务设置主模型，再点击“设为所有任务的备用模型”，确认兼容项成功更新，未配置主模型、主备相同和模型类型不同的任务被跳过并显示准确中文原因，其他绑定不受影响。
- 以项目负责人、普通成员、跨项目成员分别调用批量接口，确认只有当前项目负责人可修改；归档项目、停用模型及停用接入渠道均应整体拒绝，不能产生半成功绑定。
- 在常用桌面宽度及 125%/150% 缩放下复核一键应用区域的模型下拉框、两个批量按钮、确认弹窗和完成摘要；窄屏应改为单列且不出现横向溢出。

## V1 Scenario Flow Editor 主流程待集中验收

- 在真实浏览器打开测试编排页，确认默认显示流程图，可切换列表并保持同一份未保存 DSL；新增、重命名、启停、失败策略、详细配置在两个视图之间应立即一致。
- 构造顶层、IF/ELSE、LOOP 和两层嵌套节点，确认实线顺序、虚线父子作用域、类型色彩、禁用态和校验错误标记准确；画布横向滚动不得遮挡或截断深层节点。
- 拖动普通节点到首个普通节点、END 前和跨父子作用域位置，保存后核对 DSL 顺序与 Runner 执行顺序一致；START/END 不可拖动、复制或删除，父节点下拉不得允许自身或后代形成循环。
- 从流程图右侧属性面板分别执行详细配置、测试节点、运行到此、从此处运行，确认继续复用既有离线 Preview；保存新版本、恢复 AI 原始编排和批准执行不应因视图切换改变语义。
- 在常用桌面宽度以及 125%/150% 缩放下复核节点、连线、右侧属性和工具栏；在窄屏确认属性面板落到画布下方，键盘选中节点和 Enter 操作可用。当前自动浏览器连接返回 `nodeRepl.fetch request failed`，因此本项尚无真实页面证据。

## V1 Prometheus 观测主流程待集中验收

- 已完成本地隔离门禁：显式 v2.55.1 镜像、本机 19090 绑定、真实抓取、Backend 停止/恢复时 `up=1→0→1`、Prometheus 重启后命名卷历史样本保留。以下仅保留 Linux/服务器、完整业务 Counter 与发布告警策略等环境项。

- 使用显式版本的 Prometheus 镜像分别启动开发与服务器 Compose，确认 `up{job="ai-test-backend"} == 1`、配置重载无误、默认仅 `127.0.0.1:9090` 可访问，且 `/metrics` 不进入公开 OpenAPI。
- 在 Windows 本地和 Linux 服务器分别核对进程 CPU/RSS、整机 CPU/内存利用率与 1/5/15 分钟 Load；制造请求后核对 HTTP 请求量、延迟直方图与在途量增长，动态路径只允许规范化路由模板，未知路径只能标为 `unmatched`。
- 创建普通/性能 Run，完成 PASS/FAIL/无 SLA 性能任务，执行 AI 调用与 Webhook 成功/重试/失败，核对对应业务 Counter、Token 与估算成本增量；任何指标标签不得出现项目、用户、Run ID、URL、Token、Secret 或任意错误正文。
- 暂停 Backend、恢复 Backend，并短暂制造抓取超时，确认 Prometheus target 正确转为 down 后恢复，应用请求不受抓取失败影响；验证重启 Prometheus 后数据卷仍保留，开发 7 天和服务器默认 15 天保留参数生效。
- 结合目标环境容量制定告警规则与展示面板，至少覆盖 Backend down、错误率、P95、性能 SLA 失败、AI 失败、Webhook 失败、CPU 和内存；本批未引入 Grafana，后续如需展示层应另行定义权限与发布边界。

## V1 AI Performance Analysis 主流程待集中验收

- 在独立临时库验证 `0076 → 0077`、空库升级及 downgrade 往返；不在生产库演练 downgrade。核对分析表的 Run/Prompt/Schema/AI Call 外键、单 Run 草稿唯一约束、快照大小/hash/verdict CHECK 和历史只读追溯。
- 配置真实 `PERFORMANCE_ANALYSIS` 模型绑定，对满足 SLA、未满足 SLA、未配置 SLA、SSE/LLM 和含错误分布的性能 Run 分别生成分析；确认 verdict 永远等于平台原判定，finding 指标和值逐项可在来源快照复核。
- 让模型尝试改写 SLA、编造指标、引用来源外指标、输出 URL/凭据、超长文本或不符合 Schema 的内容，确认最多一次 Repair 后仍不合法则不保存完成分析，原性能指标和 SLA 记录始终不变。
- 同时对同一 Run 发起两个生成请求，确认只有一个草稿进入模型调用；模拟进程在 DRAFT 后退出，10 分钟后可恢复，不永久阻塞。AI Call、Prompt Version、Output Schema、实际模型、Fallback/Repair 和快照 SHA 应可追溯。
- 以项目只读成员、可写成员和跨项目用户验证历史读取/生成权限；补充指令按不可信输入处理，接口、日志和分析文本不得泄露目标 URL、Header、Cookie、Token、Secret 或原始响应。
- 浏览器复核性能页“AI 分析”抽屉、Prompt 选择、长耗时 loading、空态、错误提示、历史分析、人工复核标记、指标发现、建议和瓶颈假设在常用宽度及 125%/150% 缩放下可用。

## V1 CI/CD API 与 Webhook 主流程待集中验收

- 在独立临时库验证 `0075 → 0076`、空库到 `0076` 以及 downgrade 往返；不在生产库演练 downgrade。
- 以项目 Owner 创建全项目 Token 与限定 Test Plan Token，确认明文只出现一次、数据库只保存摘要、到期和撤销立即生效；普通成员、跨项目计划和越权 Plan Run 均不可访问。
- 从 GitHub Actions、GitLab CI 或本地等价流水线使用 Bearer Token 和唯一 `Idempotency-Key` 触发；同键重放只能返回原 Plan Run，不重复创建或投递，流水线引用和合法 Commit SHA 可追溯。
- 配置真实公网 HTTPS 接收器，核对终态 JSON、Delivery/Event/Timestamp 头和 `timestamp.canonical_json` 的 HMAC-SHA256；管理页、日志与错误不得回显完整 URL 查询、签名密钥或 CI Token。
- 分别制造 2xx、4xx、429、5xx、DNS 失败、连接失败和超时，确认只对可恢复错误指数退避，达到上限后进入最终失败且可人工恢复；模拟投递进程在 `SENDING` 后退出，90 秒租约到期后应恢复。
- 验证 HTTP、userinfo、fragment、环回、私网、链路本地与解析到非公网地址均被拒绝，HTTP 3xx 不跟随；解析与连接目标一致性仍需在真实网络环境做 DNS rebinding 对抗复核。
- 在没有打开前端、CI 客户端也不轮询状态时完成子 Run，确认后台协调器仍聚合 Test Plan 终态、只生成一条事件并完成投递；两个 Backend 实例并发扫描不得产生重复 Outbox 事件。
- 浏览器复核“CI/CD 集成”导航、Token 一次性展示与复制、计划多选、撤销、Webhook 密码输入、归档恢复、投递状态和重试在常用宽度及 125%/150% 缩放下可用。

## V1 Scheduler 主流程待集中验收

- 在独立临时库验证 `0072 → 0073 → 0074` 和空库到 `0074` 的升级及 downgrade 往返；不在生产库演练 downgrade。
- 分别创建 Daily、Weekly 和 Cron，核对 Asia/Shanghai、UTC 及一个带夏令时的 IANA 时区；不存在的本地时间应跳过，重复本地时间只能触发一次。
- 验证 Cron 的 `*`、列表、范围、步长、周日 0/7 和日/周组合语义；非法字段、越界值、0 步长、非 5 段表达式必须在保存或预览时拒绝。
- 同时启动两个 Backend 扫描同一到期任务，确认 `schedule_id + scheduled_for_at` 只产生一条 Trigger 和一个 Test Plan Run；模拟停机错过多个周期，恢复后只合并触发一次且下次时间进入未来。
- 到点前归档计划、停用创建人、移除项目权限、关闭 Environment、使 Runner 离线或 Slot 不足，确认不创建不可执行 Run并在 Trigger 中留下脱敏错误；修复后重试成功且不重复既有成功触发。
- 浏览器复核“定时任务”导航、空态、三种类型切换、星期选择、Cron 帮助、未来五次预览、启停/归档、下次/上次时间、触发历史及失败重试在常用宽度和 125%/150% 缩放下可用。

## V1 Test Plan 主流程待集中验收

- 在独立临时库验证 `0071 → 0072` 和空库到 `0072` 的升级，并验证 downgrade 往返；不在生产库演练 downgrade。
- 分别创建纯 API、纯 Scenario、纯 Web 和三类混合计划，确认保存时固定当前版本；计划编辑或目标发布新版本后，既有计划运行仍使用启动时的 Plan Snapshot 与子 Run Version。
- 以普通项目成员验证创建、编辑、归档/恢复、执行前校验和立即运行权限；跨项目 Environment、Runner 目标、版本 ID 和计划 Run 均必须拒绝或不可见。
- 使用非敏感变量驱动 API URL/Body、Scenario 初始变量和 Web URL/Action，确认变量随子 Run 固化；敏感命名、保留字段、非法变量名、超过 50 项或 64KB 的输入必须在投递前拒绝，接口和日志不得回显 Secret 值。
- 让其中一个目标缺 Capability、Slot、Chrome、Environment 或固定版本，确认整批执行前校验失败且不创建任何子 Run；修复后再次运行应一次创建全部子 Run。
- 制造 RabbitMQ 首次发布失败，确认计划运行保留子 Run 和安全错误，可通过“重试投递”恢复且不重复创建；运行中点击取消，应只取消非终态子 Run并正确收口聚合状态。
- 分别组合全成功、成功+失败、全取消、全超时及排队/运行混合状态，确认计划聚合统计、终态和结束时间准确，历史错误与子 Run 编号可追溯。
- 浏览器复核“测试计划”导航、空态、创建/编辑弹窗、100 项上限、排序/启停、JSON 错误提示、Runner Slot 摘要、校验失败详情、最近运行轮询和详情抽屉在常用宽度与 125%/150% 缩放下可用。

## V1 性能测试主流程待集中验收

- 已完成本地隔离发布主链：独立 MySQL 空库/`0076↔0077`/head 往返；PERFORMANCE Slot 与 API/Scenario 路由；Python HTTP 的 2xx/418/503/超时/网络断连；Warm Up；SSE TTFT/Chunk；JMeter 固定迭代；SLA；历史及双 Run 对比。以下仅保留更宽负载矩阵、断线/取消/清理矩阵和浏览器交互门禁。
- 本地开发库已非破坏性完成 `0067 → 0068 → 0069 → 0070 → 0071 → 0072 → 0073 → 0074 → 0075 → 0076 → 0077` 前向迁移；独立临时 MySQL 的空库升级和 downgrade 往返也已通过，未在生产库演练 downgrade。

- 当前 Frontend 源码已在隔离临时目录完成 production build；默认 `frontend/dist` 被运行中进程占用，待停止开发服务后重新构建到默认目录，并清理不再被新入口引用的旧哈希产物。
- 在常驻 Runner 重新加载新代码后，以 `--performance-slots 1` 确认 PERFORMANCE 可用，分别使用 API Case 和 Scenario 走“性能测试 → 新建配置 → 选择环境/Runner → 启动 → 指标/对比 → 运行中心”真实闭环。
- 使用本地可控 Mock 分别验证 2xx、4xx/5xx、网络错误和超时；核对 Warm Up 不计入样本，请求数、成功/失败、RPS、Min/Max/Avg、P50/P95/P99、接收字节与错误分布准确。
- 分别制造错误率、成功率、RPS、平均/P95/P99 和 TTFT P95 阈值边界，确认 Backend 根据原始样本确定性判定 PASS/FAIL，Runner 或后续 AI 不能提交或覆盖汇总与 SLA。
- 使用固定 RPS 分别执行低速和高于目标处理能力的负载，核对计划请求数、发起节奏、实际 RPS、最大并发限制与总超时；请求数必须等于 `ceil(target_rps × duration_seconds)` 且不超过 10000。
- 分别验证固定并发/时长和多阶段 Step Load；样本达上限时停止新发起但保持设定压测窗口，阶段切换、峰值 VU 与按秒趋势一致。
- 用可控 SSE 与 NDJSON 目标核对 TTFT、流时长、Token/s、Chunk、最大 Chunk Gap、完成标记缺失及 10MB 上限；用 JMeter 分别验证固定迭代和固定 RPS。
- 分别使用 NONE、RESOURCE_CLEANUP 和 BATCH_CLEANUP，确认预热迭代自身安全清理且不进入样本，批量清理按 LIFO 执行、耗时不污染压测窗口，失败时运行明确收口。
- 验证隔离执行中的协作取消、Force Stop、总超时、Runner/Backend/RabbitMQ 短暂断线和重复 completion；确认失去响应的子进程在有界等待后终止，Consumer 重建后不重复施压，且 PERFORMANCE Slot 最终释放。
- 浏览器复核 API/Scenario、三引擎、四负载模型、阶梯编辑、Cleanup、环境/Runner 能力筛选、空态、运行轮询、取消/强停、指标抽屉、多条 SLA 标签和 2～5 Run 对比在常用宽度与 125%/150% 缩放下可用。

## 运行中心批量创建与执行待集中验收

- 在运行中心切换 API 用例，按名称和业务编号筛选并跨筛选结果多选，验证全选/清空、已选数量及公共环境/Runner 配置正确；混合一条合法与一条非法用例时，合法项应创建独立 Run，非法项应显示对应失败原因且不阻断其他项。
- 一次创建超过当前分页大小的 API 用例运行，点击“一键运行待执行项”后确认扫描的是当前项目全部 `CREATED` 记录而非仅当前页；确认数量后每条 Run 均独立投递并按 Runner 槽位排队，已执行、已完成和失败记录不重复投递，部分投递失败时列表刷新且可继续处理。让第一条用例产生业务失败，确认第二条已提前处于 `QUEUED` 且 Runner 自动继续执行，不要求页面保持打开或人工再次点击。
- 在桌面常用宽度和 125%/150% 缩放下确认测试对象切换、公共环境/Runner 两列、全宽多选框、操作按钮及批量失败清单不重叠、不横向溢出；创建后不再出现与运行列表重复的顶部状态横幅。

## AI API 用例可直接执行待集中验收

- 分别使用至少两个支持结构化输出的不同模型重新分析 Demo 根需求，确认平台可信系统契约始终随请求发送；完全缺少 `check_point_keys`、一个超长检查点合并多个分支、重复编号、越界 API 和不存在检查点应向 Repair 返回精确字段路径，仍不合格时显示黄色“规则补全完成 / 平台规则候选”。让模型仅关联登录而遗漏商品、用户、订单、资源和下载等检查点，确认平台按 OpenAPI 逐项补齐对应接口，无法匹配的检查点明确进入缺口，不能再出现“30 个检查点只靠下载和登录两个 API 仍显示成功”。
- 使用 Demo 完整根需求复测超过 30 条原子检查点的设计输出，确认 Schema、Repair、平台规则候选和后续用例生成均能保存并引用 `CP-31`～`CP-100`；重点复现历史调用中“13 个 API、57 条关联、引用到 CP-58”的结构，确认不再因旧的 30 条平台上限产生孤立引用。
- 重新加载 Backend 后，在历史 AI 生成结果中点击“用最新平台规则修复已保存用例”，确认全过程不产生新 AI 调用；未人工编辑且只有初始版本的正式用例新增一个不可变版本，人工编辑、已有后续版本、草稿和已拒绝建议均被跳过且旧版本不变。重复点击不得继续产生相同编译器版本。
- 对 Demo 受保护 API 历史用例执行上述重编译，确认登录用户名/密码来自当前项目 Secret，负向错误密码仍是一次性无效值；对 flaky/幂等键用例连续运行两次，确认每次使用不同的安全动态键且结果不受前次状态污染。
- 构造过滤但无 Query、重试但重试次数为 0、重复操作只有一次请求、要求 Cookie/Content-Type 但缺少对应断言、以及“成功和失败”复合检查点，确认平台移除不成立的 `coverage:*` 标签并写入缺口，结果页覆盖率不再按 AI 自报显示 100%。

- 在项目设置配置默认环境 Base URL，并通过“配置 AI 测试登录账号”保存合成账号；确认网络响应、AI Prompt 审计、用例 JSON 和日志只出现 `{{secret.AI_TEST_USERNAME}}` / `{{secret.AI_TEST_PASSWORD}}` 引用，不出现真实值。
- 使用真实模型对 Demo 完整根需求重新执行 AI 测试设计和用例生成，确认最终调用收到20条完整需求而非只有父标题；登录拆成正确凭据、错误用户名、错误密码、缺少用户名、缺少密码及未认证访问，订单和资源异常状态码不再被“或”合并。
- 确认受保护接口自动携带 `GET_TOKEN` 前置请求、`$.data.token` 提取和 Bearer Runtime 引用；错误密码使用 FAKER 变量。缺少 Base URL 或密码 Secret 时应在模型调用前显示单一项目配置提示，不产生不可运行建议或额外模型费用。
- 构造明文密码、不存在的 Secret、未认证受保护请求、无来源 `order_id/resource_id`、标题有过滤但无 Query、标题有重试但重试次数为0、标题要求正文但只有状态码断言，确认生成领域校验触发至多一次 Repair，仍失败时不保存建议。
- 将通过门禁的登录、受保护查询和删除/创建后查询建议保存为正式用例，在运行中心选择默认环境直接执行，确认无需再次编辑即可下发 Runner 并得到断言结果；多请求用例应按 `GET_TOKEN → API_SETUP → 目标请求` 顺序执行，从真实响应提取业务 ID。查询/校验目标使用 ALWAYS Cleanup，目标请求本身负责删除时使用 ON_FAILURE 兜底，成功、失败和取消路径均不得遗留临时资源。确认目标请求 401 时只刷新 `GET_TOKEN`，不会重复执行 `API_SETUP` 创建数据。
- 复测现场失败任务 #9：包含 `{{fake_order_id}}`、`{{fake_resource_id}}` 的删除不存在对象负例应通过 API 契约与执行门禁；再分别制造缺 Secret、受保护 API 缺认证和前置变量逆序，确认一次 Repair 收到具体 `$.cases[n].字段` 原因，仍失败时页面显示“结构或可执行性约束”而非笼统 Schema 错误。
- 在 Runner Worker 启动前停止 RabbitMQ，启动 Worker 后确认进程保持等待且退避不超过 30 秒；恢复 RabbitMQ 后无需重新注册即自动上线并消费任务。运行中分别重启 RabbitMQ、Backend/Redis，确认 RabbitMQ 消费和 HTTP 心跳自动恢复；撤销 Runner credential 时应停止并要求使用新 Registration Token，不能自动绕过撤销。
- 浏览器复核 AI 用例审核质量区显示“执行上下文完整”，旧的明文凭据、缺少认证和无来源变量不再获得该项分数；项目测试账号快捷弹窗在常用宽度与125%/150%缩放下可用。

## Web AI 规划与 Playwright MCP 待集中验收

- 分别以“可视化探索”和“后台无界面”投递同一候选：确认可视模式在所选交互式 Windows Runner 电脑弹出 Chrome、结束或停止后自动关闭，后台模式不弹窗；探索列表模式标记与执行计划一致。另以 Windows 服务或无交互桌面会话复核可视模式的明确失败提示，不得悄悄降级或把窗口误认为会显示在平台浏览器所在电脑。
- 在项目密钥管理中配置 `AI_TEST_USERNAME` / `AI_TEST_PASSWORD` 后重新加载 Backend 与 Runner，用真实登录页执行正向及错误凭据探索；确认页面“不要输入真实凭据”等被测文案不会再触发首步 `FINISH`，AI 调用只出现 `{{secret.*}}` / `{{faker.*}}` 引用，Runner 能完成输入与登录点击，而页面快照、标题、URL、网络摘要、Locator、动作轨迹和完成结果均不出现真实账号密码。交叉引用用户名到密码字段或把凭据引用到普通字段必须被 Runner 独立拒绝。
- 由上述探索重新生成校准草稿，确认冻结输入明确列出允许的 `AI_TEST_*` 引用，模型不能编造 `DEMO_*` 或其他名称；未确认缺口逐条可见。分别在 AI 首轮、Repair、人工编辑、接受校准、批准 Web Case 和创建 Run 阶段构造不存在、停用及环境不匹配的 Secret，确认在各自最早可判断阶段阻止，并且错误只显示安全的引用名称、不回显值。
- 未配置项目测试账号时打开登录类候选，确认弹窗明确显示“登录凭据待配置”并在投递前阻止；复核历史首步 `FINISH` 记录展示为“受限结束”及具体原因，不能直接生成校准草稿。非登录候选默认不下发真实凭据，仍可使用合成错误值完成负例。
- 从固定需求文档版本选择整份需求或子树，并选择零到多个同项目 API；生成期间发布新需求/OpenAPI 版本，确认旧任务快照与 SHA 不变，AI 引用范围外 ID 时任务失败且不保存候选。
- 在 Prompt Center 和项目模型绑定中分别配置 `WEB_TEST_PLAN`、`WEB_EXPLORATION_DECISION`、`WEB_PLAN_RECONCILE`；使用真实模型验证结构化输出、一次 Repair、失败审计与安全错误脱敏。
- 在 Web 测试设计加载完成并存在后台任务时，分别让 Runner 状态、某一 Prompt 查询和候选详情请求发生一次超时；确认其他已成功内容继续显示，静默轮询不弹全局“工作台加载失败”，手动刷新仅指出失败分区，依赖恢复后错误自动消失。
- 从候选条目进入人工录制，确认 `plan_item_id` 保留；完成录制后返回 AI 测试设计生成校准草稿。另走 MCP 自动探索路径，确认严格信封、Runner claim/start/control/complete、停止/取消、最大 50 步和断线重投幂等。
- 在合成测试站点验证 MCP 只使用白名单工具；构造跨 origin 导航、任意脚本/evaluate、文件上传、Cookie/Storage、敏感字段、Enter、保存/提交/删除/购买/付款/权限修改等动作，确认 Runner 拒绝且错误不包含凭据。部署侧另验证 Runner 出站网络隔离，因为上游 allowed-origins 并非安全边界。
- 分别用 MCP 探索和人工录制生成事实校准，人工编辑后接受/拒绝；确认接受只创建或更新 DRAFT Web Case，需求覆盖关联正确且事务失败不留下半成品，未经过人工批准不能运行。
- 本地开发库已从 `0063` 前向迁移至 `0067`；集中验收时核对 `0064` 四张 Web AI 新表、`web_recordings.plan_item_id`、`0065` Prompt 作用域/项目覆盖字段、`0066` API Case 意图编译器及 `0067` 探索凭据开关，并在独立临时库验证空库升级和 downgrade 往返。不得在生产库演练 downgrade。
- Windows Runner 使用 Node.js 20+ 执行 `runner/mcp_node/npm ci --ignore-scripts`，核对锁定 `@playwright/mcp 0.0.80`、本机 Chrome 启动和工具清单；升级版本前必须重新做协议与安全审查。

## 已完成的集中验收

- Backend：725 collected，724 passed，1 conditional skipped。新增 Swagger AI Case/Scenario、AI Case 批量审核、契约回链、脱敏和迁移契约均在全量内。
- Runner：518 collected，516 passed，2 conditional skipped。条件跳过是环境互斥分支，对应 Fernet/Windows 路径已有可执行分支证据。
- Frontend：`npm run type-check` 通过；Vite production build 成功，产物写入独立验收目录后精确清理，不覆盖被占用的默认 `dist`。
- 执行与部署：既有 Demo 8 项通过，含真实 headless Chrome；服务器 Compose 使用合成非敏感值执行 `config --quiet` 通过。项目内业务编号迁移后 Alembic 代码 head 和当前开发库均为 `20260912_0056`，计数器表及 Demo 需求编号已核对。
- 历史真实证据继续有效：MySQL 迁移往返、RabbitMQ 断线恢复、Redis/SSE、MinIO Evidence、Windows Runner/Chrome/Force Stop、真实 Web AI 录制与自愈闭环、报告与 Dashboard。

## 发布时必做（需新的环境授权）

1. 在首次发布前创建独立生产 MySQL，从空库执行到固定代码 head `0067`；不得把 `.local-backups/` 的开发验收数据导入生产环境，也不在生产库演练 downgrade。
2. 在目标 Linux/Docker 主机固定镜像 tag/digest，放置独立 JWT 密钥与只读 Fernet key file，按《服务器部署运行说明》执行镜像构建、迁移、显式管理员初始化和启动。
3. 用现场真实 Provider/Prompt/Model Binding 各执行一次 Swagger Case/Scenario、API/Scenario AI 断言和 Web 失败分析；仅检查脱敏后展示和审计回链。
4. 用部署域名复核 CORS、nginx API/SSE/上传、Windows Runner 的 Backend/RabbitMQ 可达性、私有 MinIO 下载和权限隔离。

## 2026-09-12 项目工作区界面待集中复核

- 分别通过 AI 建议“保存为正式用例”和测试用例模块“新建用例”创建内容等价的 API Case，确认列表仅以“AI 生成 / 人工创建”区分来源，而编号、ACTIVE/V1、详情字段、创建新版本、运行选择、报告回链和归档行为一致。人工创建时分别选择多个需求，确认每个需求的正向覆盖及用例详情反向关联均只有一条有效链接。
- 使用管理员及普通项目成员各走一次“项目门户 → 进入项目 → 需求/用例/运行/报告”的真实浏览器路径，确认项目切换后不残留上一项目的查询、抽屉或筛选状态。
- 在项目门户验证项目资料编辑、成员新增/改角色/移除和归档恢复；确认普通无管理权限成员看不到治理操作，项目创建者不能被移除或取消负责人角色。
- 进入项目工作区确认顶部不再出现重复配置按钮，左侧“运行配置”仅包含环境、环境变量、密钥、MySQL 连接和模型绑定，不再出现成员治理。
- 逐项检查项目工作区内隐藏重复项目选择器后，环境、运行 ID、状态等业务筛选仍完整可用；同时复核旧平铺地址仍能独立打开。
- 用 AI 主演示自动初始化项目验证“开始 AI 主演示”“查看测试用例”“查看 AI 审计”均落在同一项目工作区；顶部快捷“新建测试用例”应直接打开五步编辑器。
- 以管理员在平台 Prompt 中心修改一条“系统默认”模板，确认不出现 V2/V3 且所有未定制项目立即继承；点击“恢复系统初始值”后内容回到代码内置出厂模板。进入项目工作区“项目提示词”，保存两次项目定制应形成项目 V1/V2，其他项目不受影响；恢复系统默认后历史仍可查看，后续 AI 调用自动改用公共模板且不新增 AI 调用记录。
- 在项目工作区“项目提示词”准备超过 10 条模板，确认默认每页 10 条，总数、翻页和 10/20/50/100 每页切换与平台 Prompt 中心一致；翻页不应丢失已保存内容，存在未保存修改时切换模板仍需确认。
- 重置 AI_DEMO 后确认平台系统默认模板内容保持不变，Demo 页面不再把 Prompt/Schema 作为待初始化或待恢复的 Demo 资产；如果 Demo 曾有项目提示词定制，重置后应回到继承系统默认。
- 将浏览器系统时区设为 Asia/Shanghai，确认 AI 调用日志中 UTC 记录按本地时间显示（不再少 8 小时）；抽查项目、需求版本、数据集/用例版本、运行、报告、Runner、Web 会话和资源清理时间一致。再用携带 `Z` 或显式偏移的 API 时间确认不会重复偏移。
- 创建一条关联需求的 API 用例运行并打开报告：确认页面仅显示运行编号、需求/用例业务编码、名称、自然顺序和 `Vn`，不出现 `CaseRun #N`、资产/版本/步骤数据库主键；“需求关联”应把 AI 置信度解释为生成时建议值且不与通过率混淆。展开请求配置快照，确认读取执行锁定版本而不是当前版本，Method、URL、Query、Header、Body、Auth 可见，密码、Authorization、Cookie、Token 等真实值均不回显。

## 2026-09-12 AI 兼容模式待真实服务商复核

- 在项目同时配置 `AI_TEST_USERNAME`、`AI_TEST_PASSWORD` 且 OpenAPI 登录用户名没有 example 的情况下重新生成受保护 API 用例，确认登录前置请求分别引用正确 Secret；再用内置 Demo 重置后的默认资产生成，确认 username 使用 OpenAPI 的 `demo`，不会生成 `test-username`。分别让登录返回 401 和返回 200 但不含 Token，确认报告显示“登录前置请求被拒绝”和“登录响应中未找到 Token”两个不同中文原因。
- 使用当前真实模型连续执行至少 10 轮 API 用例生成，确认 AI 审计中输出的是紧凑 `intents + gaps` 而不是完整 DSL；结果页应显示意图数、平台编译成功数和跳过数。人工制造一条缺少响应 Schema/依赖闭环的意图，确认它进入缺口但同批其他用例仍保存；抽取成功用例经正式化后在真实 Runner 执行，核对登录 Token、业务 ID 前置提取、内容断言和 Cleanup 都由平台生成且生效。
- 使用当前实际模型服务重试需求 AI 用例建议；若服务商拒绝原生 JSON Schema，确认后端只执行一次兼容请求，最终建议仍通过 Output Schema。
- 需求“AI 用例”点击后应立即返回任务记录，弹窗关闭且页面不占用长请求；验证 `QUEUED → RUNNING → SUCCEEDED/FAILED`、每 3 秒刷新、离开后返回查看和“查看结果”回链。
- 在同一需求已有进行中任务时再次点击，确认前后端都阻止重复创建；失败时只显示脱敏错误摘要，不泄露服务商正文或 API Key。
- 对仍使用同步模式的其他 AI 功能，用响应时间超过 15 秒但不超过 180 秒的真实模型验证长请求等待；同时抽查普通页面请求仍维持 15 秒上限。
- 分别制造模型绑定缺失、Prompt 类型不匹配、HTTP 400、超时和结构校验失败，确认前端展示对应安全错误摘要，且不会回显 API Key 或服务商响应正文。
- 让兼容模型分别返回 `json_path + EQ` 与 `json_path + CONTAINS`，确认只规范化为 `JSONPATH_EQUAL` 和 `CONTAINS + JSONPATH`；其他未知断言仍触发领域校验/一次 Repair，不能直接保存。
- 在内置 Demo 已导入 OpenAPI 的前提下从空状态生成 AI 用例，确认新建议的请求方法和 `{{base_url}}` 地址命中项目 API 定义，且同时包含有效断言与响应内容断言。故意让模型遗漏请求或编造地址，确认一次修复后仍不合格时任务失败、不会保存半成品。
- 使用当前真实模型重新执行 API 页“AI 场景编排”，确认输出使用 `dsl.version + dsl.nodes + dsl.settings` 并形成待审核建议；让模型首轮返回旧 `dsl.steps`、`${token}` 或编造路径时，确认领域校验触发至多一次 Repair。最终仍失败时 AI 审计保留失败记录，弹窗内持续显示安全原因；成功接受后只创建 DRAFT Scenario，仍需到“测试编排”人工检查和批准。
- 打开 API 页“AI 场景编排”，依次验证三阶段：第一阶段默认选择最新完整需求版本，可切换历史版本并选择整份需求或任一需求子树，子树必须自动包含全部下级；第二阶段只生成可多选的场景方案目录，历史显示等待、运行、完成或失败；第三阶段把所选候选分别生成独立完整 DSL，单条失败不得影响其他结果。用 Demo 整份需求规划时确认模型收到 `demo` 登录账号和全部子需求，优先引用已存在 Secret 名称而不读取真实值；生成期间再发布需求新版本不得改变旧任务来源快照。
- 在 API 页“AI 场景编排”的方案目录确认“一键全选未生成项”只选择从未生成的候选；已经生成过的候选有独立卡片样式和历史状态，成功/进行中不能重复选择，失败或已拒绝仅能人工勾选重试。再选中含大体积输出的建议并展开“脱敏后的 AI 原始输出”，确认 JSON 已格式化、查看器内部独立滚动，弹窗在常用桌面宽度及 125%/150% 缩放下不横向漂移、不越过视口，收起后编辑区和操作按钮位置恢复稳定。
- 接受一条 API 场景建议后，确认结果区显示场景业务编号 `SC-xxxxx`、名称、V1 和草稿状态，不再把数据库主键显示成“场景 3”；建议历史应显示名称、说明、OpenAPI 来源版本、节点数、生成时间及 Repair/Fallback 标识。点击“前往测试编排”应进入同一项目并自动选中刚创建的场景。
- 在 OpenAPI AI 场景编排弹窗点击“全屏”，确认弹窗占满视口、长编辑内容仍在弹窗主体内滚动且关闭按钮可用；点击“退出全屏”恢复原尺寸，直接关闭后再次打开也应保持普通模式。
- 从已存在旧版 Demo 的开发库进入“AI 主演示”，确认页面不再重复输入模型地址、模型标识或 API Key，而是列出模型中心已启用的文本模型；选择模型后必须先验证真实连接，通过后才补齐新版素材并绑定全部 AI 任务。无可用模型时应明确引导到模型中心，失败时不创建半成品项目。页面应显示 20 个需求章节和 14 个 API 操作；当前本地 Demo 应无旧 AI 用例生成记录和 AI 正式用例。后续重新生成的结果不应因模型中心配置更新而被自动删除。
- 在模型中心编辑一个已配置凭据的接入渠道，确认弹窗显示“已配置”和脱敏 Key 摘要，不会因输入框为空误解为未配置；直接保存应保留原 Key，输入新值才轮换，点击清除并保存才移除。接口和页面不得返回完整 API Key。
- 在项目模型绑定中使用最大输入 4K 的数学/翻译等专用模型，确认 API 测试设计、完整需求评审、用例/场景生成等大上下文任务无法选中且服务端拒绝绕过前端的单项绑定；“一键应用”不得把容量不足模型覆盖到全部任务，批量结果应逐项说明跳过原因。对历史不兼容绑定应显示集中警告，改选 16K 以上通用模型后消失；再用完整 Demo 需求执行 AI 测试设计，确认不再因上下文不足返回 HTTP 400。
- 在非生产环境以管理员打开 AI 主演示的“重置主演示数据”，核对删除分类统计、保留项和 `AI_DEMO` 二次确认；存在运行中 Run、Web 录制或后台 AI 任务（含场景方案规划）时必须阻断。重置后项目 ID/成员、全局模型与加密 API Key、平台级系统默认模板应保持，项目派生数据及私有 MinIO Evidence 应清空，20 个需求章节、14 个 API、与当前系统任务目录等量的模型绑定及公开合成运行凭据应恢复；另一个项目的数据不得变化，生产环境接口应不可见。
- 从任意 AI 评审进入需求修订工作台，确认业务标题和默认版本说明只显示固定来源“完整需求 Vx”，不再出现由数据库 `review.id` 拼成的“第 N 次评审”；数据库编号只能在明确的技术追踪信息中出现。
- 在两个独立项目中分别创建需求、测试用例、场景和 Web 用例，确认每类业务编号都从 1 开始且互不影响；删除中间资产后继续创建，确认编号递增而不复用。抽查运行、报告、评审和关联页，主视图只展示业务编码、名称和版本号，内部记录号仅在技术追踪区出现。

## 2026-09-12 AI 设计后台交互与分页待集中复核

- 从项目门户进入任一项目，确认路由进入独立项目工作区外壳：左侧只出现项目目录，顶部只有一行项目栏；平台标题和“开发管理员”等账号入口不在项目内重复显示。点击“项目门户”返回后，平台公共目录与账号菜单恢复。
- 确认项目顶栏不再出现“新建测试用例”；从左侧“测试用例”进入后仍能正常新建用例。切换当前项目应回到目标项目概览，需求/API/用例等项目上下文不得残留上一项目。
- 确认需求页通用操作区不再出现游离的“AI 设计记录”；右侧“AI 评审”“AI 用例”页签带有明确 AI 视觉标识，两个功能卡在窄侧栏、125%/150% 缩放和不同中文长度下不重叠、不裁切。
- 首次直接进入需求管理时确认不自动选中需求，两个 AI 页签均不可用且出现“先选择需求”提示；使用仅携带旧 `requirement_id` 的普通地址进入时，即使中间详情被装载也仍不可解锁 AI。点击左侧节点后应同步高亮并解锁。另从 API 定义携带指定需求进入，确认成功提示不再隐藏工作台，并自动打开该需求的 AI 测试设计。
- 分别为两个需求生成 AI 评审，在“评审记录”中确认项目级分页、需求编号/名称/版本、状态和总体结论正确；“查看详情”应切换到对应节点和 AI 评审页签并覆盖打开该条评审，关闭详情后仍停留在评审记录列表。
- 不选择需求时从页面顶部点击“AI 记录”，确认居中弹窗默认显示项目全部需求，并可切换“评审记录 / 设计记录”；选中某一需求后从右侧点击“本需求评审记录”或“本需求设计记录”，确认同一弹窗只显示该需求的数据且范围标题明确。
- 在评审草稿点击“确认评审”，确认先出现作用说明；确认后人工编辑被保存、状态冻结为已确认，需求正文和完整文档版本不发生变化，也不自动创建测试用例。再次打开该记录应出现“基于评审创建新版本”。
- 为根需求发起 AI 评审，确认提交后立即产生后台记录且可关闭页面；记录状态从等待中/生成中变为草稿，详情快照包含全部有效子需求，结论不再误判为“只有标题”。确认右侧页签不再重复铺历史卡片，“查看详情”可直接编辑、接受或拒绝。
- 删除一条已完成或失败的 AI 评审记录应成功，需求与 AI 调用审计仍保留；删除等待中或生成中的评审任务应被后端拒绝并显示明确提示。
- 使用不支持原生结构化输出的 OpenAI 兼容模型执行需求评审和 AI 测试设计，确认请求包含完整 JSON Schema、合法输出直接成功、非法输出只做一次有界修复；验证超时和无效输出仍保留失败审计，不伪装为 AI 成功。
- 在需求 1 发起 AI 测试设计并立即点击“关闭，后台继续”，再切换需求 2 发起设计；确认两个任务互不覆盖，“AI 设计记录”能同时显示各自需求编号、名称、版本和状态，活动数量准确，且“查看详情”能跳转到正确节点并恢复对应设计流程。
- 从“AI 用例”功能卡打开设计记录：删除一个未被引用的成功/失败记录应成功，AI 调用审计仍在；删除运行中记录或已被用例生成引用的设计应被阻止，需求、建议和正式用例不受影响。
- 使用真实百炼制造 15～180 秒响应，确认设计任务不触发普通请求的 15 秒超时；分别验证主模型、备用模型和输出修复都遵守 180 秒上限，超时后任务保留安全错误摘要且前端不持续占用弹窗。
- 对项目、API、数据集/版本、用例/版本、AI 记录、场景、模型、Runner、运行/报告/证据、项目配置、Web 资产和 Web AI 建议历史分别准备超过 10 条记录，确认默认只显示 10 条，可换页并切换 20/50/100 条；切换每页条数后回到第一页，删除末页记录后页码自动收敛。
- 确认需求树仍为独立滚动，场景节点、用例步骤、断言、定位器和一次性导入预览不显示误导性的业务分页控件。
- 在 AI 生成的多接口场景进入测试编排，确认每个节点显示 AI 中文描述；顶部明确展示“主流程失败控制”“数据清理策略”“初始 Runtime 变量”，`base_url` 不再被误解为网址选择器。打开 Preview 应自动加载并执行按 HTTP 节点区分的默认模拟响应，登录 Token、列表 ID 和后续断言不再争用同一份响应。
- 在包含 HTTP、EXTRACT、ASSERT_JSONPATH、API_CLEANUP 等不同长度类型的场景中确认节点名称输入框、父节点下拉框、节点策略下拉框、启用开关与右侧操作按统一网格对齐；START/END 名称框应跨越空配置列，窄屏换行不得重叠、横向溢出或截断操作按钮。
- 修改 Preview Context 或任一节点模拟响应后确认显示“已修改，未保存”；关闭时可丢弃，预览通过后“保存模拟数据”覆盖当前场景版本原保存记录。新建场景版本后模拟数据应隔离；版本变化时旧弹窗保存请求必须被拒绝。
- 点击测试编排顶部“配置并预览完整流程”后确认只加载模拟数据且显示尚未执行，不自动产生执行结果；手动点击弹窗底部“运行完整流程预览”后才执行。通过后保存模拟数据，再次打开应复用该数据；外部节点 DSL 和正式 Runner 输入不得发生变化。
- 将 AI 节点显式设为 CONTINUE 且开启场景级“最终失败后停止”，分别用 Preview 和正式 Runner 确认节点最终失败后后续主流程 SKIPPED；RETRY_ONCE 仍最多执行两次，ON_FAILURE/ALWAYS Cleanup 仍执行。
- 保存一个修改后的 AI 场景版本，再点击“恢复 AI 原始编排”，确认直接恢复其接受 AI 建议时的 V1 到编辑区、变更说明自动填入且不自动保存；AI 调用日志数量不得增加。手工创建、没有 AI 来源的场景应显示明确不可恢复提示。
- 删除一个从未运行的 DRAFT 场景，确认二次提示会说明其版本与 Preview 模拟数据也会删除，删除后列表和详情均不可见；APPROVED、ARCHIVED、曾批准运行后又因新版本回到 DRAFT 的场景均不得硬删除，历史运行与 AI 调用审计保持不变并提示改用归档。
- 提交 OpenAPI AI 场景编排后确认弹窗在后台记录创建成功时自动关闭，页面不再等待模型返回；立即重新打开时应看到“等待生成/正在生成”，完成后变为“已生成 · 待审核”，失败时保留脱敏原因且允许再次生成。生成期间关闭弹窗、切换页面或重新打开均不得丢失记录或重复创建任务。
- 让 AI 场景首轮在登录请求中返回 OpenAPI 示例密码，确认共享领域校验触发一次 Repair，最终结果只能使用 `{{secret.<逻辑名称>}}`；仍返回明文时任务失败且不生成建议。人工向 HTTP Body/Header/Cookie 写入明文敏感值时，校验、保存和离线 Preview 均应在正式运行前给出节点名称和精确字段路径。
- 在测试编排“敏感凭据绑定”区分别验证选择已有 Secret、创建项目通用 Secret 并自动绑定、Secret 缺失及已停用状态。未绑定引用应可保存 DRAFT 但不能批准；完成绑定并保存新版本后才能批准。确认页面和网络响应不回显 Secret 值，环境专用 Secret 在不匹配的运行环境仍由正式 Run 校验阻止。
- 从空项目导入 Markdown，确认整份需求直接成为已发布 V1；打开“编辑需求树”，确认新增/删除固定显示在节点右侧，拖动只允许调整同一父级下的纵向顺序且不会改变层级；新增、移除并发布后只产生一个完整文档 V2，目录编号按位置连续重排，旧 V1 快照仍保留。
- 在已确认评审详情确认浅色高对比、大字号的“AI 辅助修订并创建新版”按钮清晰可读；点击后应直接打开修订工作台并自动创建后台规划，不再出现要求逐条手工选择目标需求的中间弹窗。确认内容修订按需求归类并提供原文/修订稿对比，新增与删除建议分别集中显示，可逐项或一键应用、撤销和人工编辑；规划期间关闭工作台后后台继续，再次进入可恢复等待状态或规划结果。验证“全屏查看 / 退出全屏”切换、关闭后重新打开恢复普通尺寸，以及规划完成后“关闭，不保存草稿”不会误导用户认为未发布编辑已持久化。
- 使用真实百炼模型验证完整 Demo 需求的修订规划可在任务专属 300 秒上限内完成；模拟上游超时后应显示失败原因和“重新生成规划”，重新生成产生新的 AI 调用审计记录且不会覆盖原失败记录。
- 使用真实百炼模型验证完整 Demo 的 AI 测试设计和测试用例生成可在任务专属 300 秒上限内完成。检查点超过 10 条时确认默认分页为 10 条/页，可切换 20/50/100 条并正常换页；翻页后进入 API 闭环，检查点总数和覆盖关系不得变化。
- 前向应用 `0078` 后选择完整 Demo 根需求发起 API 测试设计，确认界面明确显示总范围、API 纳入范围以及性能/Web/平台需求的自动分流；用 GLM 复测原 `#82/#83` 类型输出，模型遗漏叶子或引用不存在检查点时应由平台补全并保留可用 AI 结果，不再整份降级为规则候选。
- 在同一需求连续完成两轮 AI 用例生成，确认右侧窄栏只显示生成历史而不再直接铺开覆盖率和草稿卡；分别点击两条“查看结果”，宽幅结果中心只能出现所点轮次的用例、覆盖率、能力缺口和审核统计。验证全屏切换、10/20/50/100 分页、跨页勾选、批量保存/拒绝/删除、单条审核的上一条/下一条，以及关闭审核详情后仍返回当前结果中心。
- 当推荐 API 超过 10 个时，确认接口闭环步骤默认分页为 10 条/页，可切换 20/50/100 条。分别在不同页面勾选可选接口后，确认跨页选择不会丢失，“已选数量”按完整推荐集合统计，核心接口保持必选，能力缺口不随接口分页隐藏。
- 使用百炼模型生成包含 `EXISTS/NOT_EXISTS` 断言的用例，确认模型即使返回多余 `expected` 也能被确定性规范化并通过；同时验证未选 API 地址、无有效内容断言以及其他不安全结构仍会被拒绝，不得把兼容处理扩大为绕过领域校验。
- 使用带路径参数的已选 API 验证模型返回具体示例路径时，仅在方法一致且唯一匹配契约模板的情况下恢复为 `{{变量}}` 地址；构造零匹配和多个模板均可匹配的情况，确认仍被白名单校验拒绝。
- 确认完整树可见，但只有发起评审时所选需求及其全部下级可编辑，父级、同级和其他分支带锁只读；尝试构造范围外修改时后端应返回 409。删除子树和“仅删当前、下级提升”都能正确生成完整文档草稿，发布后只归档当前有效节点，旧版本和既有测试关联仍保留。直接“确认评审”但不创建新版时，当前完整文档版本应保持不变。

本地重建按用户明确授权执行：旧 `0038` 数据库和 MinIO Evidence 已归档，当前开发库已从 `0054` 空白基线非破坏性增量升级到 `0076`；未重启服务、未读取真实密钥内容、未运行旧验收恢复脚本。项目仍无正式生产库。

## 2026-09-13 Web MCP 通用权限与可审计探索待集中复核

- 用不同业务系统分别创建探索，验证五类权限按任务固化，不依赖 Demo 路径、页面名称或账号。关闭 `FORM_SUBMIT`、`TEST_DATA_CREATE` 或 `DIRECT_API_NAVIGATION` 时，确认返回 `MCP_PERMISSION_DENIED` 与准确缺失能力；开启后只允许对应范围。
- 使用包含 `buyer`、`buyer_name` 等普通业务文本的页面，确认不再被 `buy` 子串误判为购买动作；真实付款、权限修改、文件上传和删除仍始终拒绝。
- 在可视化和后台两种模式分别运行 UI 探索，确认 AI 首先操作可见 UI，不把 `/api/*` 当作默认替代流程。页面无对应 UI 时应明确记录 UI 能力缺口。
- 探索运行中打开“查看过程”，确认 2.5 秒轮询可持续看到页面、动作、决策依据与预期反馈；终态报告补齐 Runner 执行反馈，不显示凭据明文或模型隐式思维链。
- 在 100%、125% 和 150% 缩放下打开包含 20 步以上记录的“探索过程”，确认弹窗尺寸保持在视口内，标题与摘要固定可见，仅内部步骤区滚动，关闭按钮始终可操作。
- 勾选“保存关键截图”后分别覆盖登录失败、登录成功、下单成功、退出成功、执行失败和正常结束，确认截图绑定正确步骤、刷新后仍可加载且点击可查看大图；未授权时不得产生截图。
- 使用托管真实测试账号制造登录失败并停留在登录表单，确认 Runner 因安全规则跳过全页截图，不在 MinIO、接口响应或页面中留下用户名/密码；合成错误账号场景仍可保留错误提示截图。
- 中途关闭 Chrome 或令探索失败，确认已经上传的前序截图仍保留；模拟 MinIO 暂时不可用时，探索业务结果仍能收口，页面过程记录不因可选截图失败而消失。
- 让探索决策模型对 `TYPE/CLICK/SELECT` 只返回当前快照中的有效 `ref` 而遗漏 `element`，确认平台从快照确定性补全并继续执行；返回不存在的 `ref` 时应安全失败。再制造决策 422，确认过程弹窗至少保留第一页 `OBSERVED` 观察，错误区不再显示 request_id 或嵌套后端 JSON。
- 让探索模型把当前快照有效的 `[ref=…]` 误写进 `element` 或仅把 ref 值写成 element、同时漏掉独立 `ref`，确认平台从同一页快照恢复引用、规范化元素并直接执行而不触发 Repair；不存在、多个候选或与显式 `ref` 冲突时仍应安全失败。
