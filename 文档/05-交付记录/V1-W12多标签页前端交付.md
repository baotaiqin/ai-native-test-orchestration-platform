# V1-W12 多标签页前端交付

状态：已实现，基本检查通过，待集中验收。

- 前端共享 DSL 已增加 `NEW_TAB`、`SWITCH_TAB`、`CLOSE_TAB` 类型、工厂、摘要、别名和 URL 校验。
- 手工 WebCase 编辑器与录制 AI 建议人工编辑器均可配置、保存并回显三种动作；两处统一使用“标签页别名”及相同说明。动作类型切换沿用共享工厂重建对象，不保留上一类型的无关字段。
- `NEW_TAB` 要求合法 http(s) URL 或运行时模板，且不能重用 `main`；切换和关闭允许填写 `main`，未知、重复、已关闭别名和最后一页关闭继续交由执行现场判断。

基本检查：Frontend type-check 通过；前端真实工厂到 Backend `WebCaseContent` Schema 的正反契约测试 1 项通过；隔离 production build 通过（1782 modules）。常规 `frontend/dist` 在清理被占用的历史产物时遇到 Windows `EPERM`，未停止现有服务，改用 `.codex-validation/v1-w12/dist` 完成构建。

待集中验收：真实浏览器中的手工编辑、AI 草稿编辑/接受、版本保存/批准、三端正式加载、多标签页运行语义、截图及旧动作回归。
