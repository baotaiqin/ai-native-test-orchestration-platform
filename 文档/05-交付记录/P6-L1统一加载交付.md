# P6-L1 统一加载交付（r1 加载、r2 监听收口）

## 结论

`P6-L1/r1` 已按授权完成 Backend 与 Worker 的统一加载，原最终只读验证 **32/32 通过**。`P6-L1/r2` 又对 8765 重复监听做了只读收口：确认额外 PID `49364` 是早于 r1 存在的未归属 `python-http-server`，r1 没有创建、终止或重启它；原 Demo PID `21264` 保持不变并继续实际提供 Demo 服务。

- Backend 已加载经主控冻结的 P6-R1/r2、P6-X1/r2、V1-T2/r1 与 P5F-L1/r2 隐私修复源码。
- Worker 已加载 V1-T1/r1 源码，复用原注册身份 `1282a04dfd894f85877384cb31af5c16`，未调用注册接口。
- Frontend、已归属 Demo、MySQL、RabbitMQ、Redis、MinIO 均未重启；额外 8765 监听也未被本包触碰。
- 未创建、批准或执行测试资产；未调用 AI；未读取、发布、ACK 或重投消息；未运行旧恢复/归档工具。
- 本交付只证明服务加载就绪，不代表 Phase 6 或 V1 最终验收通过；前端最终联调仍由主控安排。

## 加载前门禁

正式前置检查文件：`.codex-validation/p6-l1/preflight.json`，SHA-256 `8d2d3c9347abd2f8619aafa9df59d3536de786bce9308e009d68a17d1e0d60dc`。

- 主控冻结文件 `.codex-validation/p6-l1-controller-freeze.json` 中 19/19 个源码哈希匹配；最终确认时间为 `2026-09-09T20:42:48.4572283Z`。
- MySQL 迁移头为 `20260909_0038`。
- 全库在途 Run 为 0，在途 Web 录制为 0。
- 唯一 ACTIVE Runner 为既有 Runner；加载前为 ONLINE、WEB READY、可用容量 1/1。
- Project 23 为 ACTIVE；Case 5/6、Page 1/2、Element 1/2 均为 ARCHIVED。
- C1 受保护历史与验收基线逐字节/逐行指纹一致：4 个终态 Run、4 个 CaseRun、4 个 StepRun、4 个 Web 执行结果、19 个 Evidence、AI Call 12..20、Proposal 2/3、Analysis 2 均未漂移。
- 成功 result 与成功 attempt 逐字节相等，SHA-256 均为 `2a9995eb77ab3710431c800bbaf5a4edb2e4485afbb730b2e71dd94860bee176`；首次失败 attempt 为 `6641011f5e02b7137191bd9ca5ab5a5049eb4099a702db4b1b8c9485da22e42b`；AI ledger 为 `eb9cbf3fd6a43b5be90d74a658d01ae73126b6f3f82e6ca1444b21c1e19f8b43`。
- redrive journal 状态仍为 `source_acked`，SHA-256 `fd53f47db14f32272e530ffcad3f7395ff60c89046d52bfb21fbb6189634125f`。
- RabbitMQ Runner 队列为 0；原 DLQ 为 messages=1、ready=1、unacked=0。

验证器首次开发运行曾按容器化 MySQL 检查，但本机正式 MySQL 不是 `ai-test-mysql` 容器，因此在任何服务变更前以 `CalledProcessError` 安全停止。随后改为以 MySQL `server_uuid`、主机、端口及单调增加的 uptime 证明未重启，正式前置门禁通过后才进入加载窗口。

## 精确进程切换

加载顺序严格为：确认 19 项哈希 → 停止已拥有且空闲的 Worker → 停止已拥有的 Backend → Backend 健康/OpenAPI 就绪 → 恢复同一 Runner Worker → 最终只读验证。

### Backend

- 旧进程树：root `29460` → conhost `22252` / listener `52512`。
- 新进程树：root `47552` → conhost `21148` / listener `56124`。
- 新进程均启动于 `2026-09-09T20:42:58Z`，晚于最终源码确认时间。
- 新 root/listener 命令身份均为 `python-uvicorn-app.main-127.0.0.1-8000`；listener 命令哈希为 `c720ba10bdc8b52f7694a2b125892b25a5e3937c62b35105477ac871b0388ab8`。

### Worker

- 旧进程树：root `23364` → child `57108`。
- 新进程树：root `62088` → child `18508`。
- 新进程均启动于 `2026-09-09T20:43:06Z`，晚于最终源码确认时间。
- 新 root/child 命令哈希分别为 `751b18249a6a20c4b19612ea9b4adbd7a1c3d37e993f7b2fdd1c074f5d155cc2` 与 `efb0366706448dfc8832f9fe0b18e4a65307d7ebc6808348069dff6f2e834594`，与原批准命令身份一致。
- RabbitMQ URL 仅从既有 Runner 配置加载到内存，并校验为本机 `amqp/amqps`、端口 5672；证据与输出未写入 URL 或凭据。

### 非目标运行态

- Frontend 5173 listener 在前后均为 PID `53572`。
- r1 的 `Get-NetTCPConnection` 视图在前后均返回 Demo PID `21264`，`changed_locator=true`；该视图只代表提供器返回的单条重复端点记录，不代表 8765 的完整监听集合，完整边界见下方 r2 收口。
- RabbitMQ 容器 ID `2138be300c7b6acedd493f5da327690c97067ce315ed463d95e8afdeed52261a`，前后 running/healthy。
- Redis 容器 ID `9a1be91806205be37fe15b22899527719abf5efd07a7c120a60d92db34701ff0`，前后 running/healthy。
- MinIO 容器 ID `b9c5ff2584200afa9964dd99b2ea141fe1276be2b6b23ffb98229e259b056277`，前后 running/healthy。
- MySQL `server_uuid` 保持 `96f8365c-8e5b-11f1-93cd-8c47be378628`，uptime 从 1913734 秒增加到 1913777 秒，证明加载窗口内未重启。

## r2：8765 重复监听只读收口

### 实际身份与时间线

- 已归属 Demo 进程树仍为 root `54536` → listener `21264`；listener 启动于 `2026-09-09T16:41:17.7881556Z`，命令哈希 `e7505b05521a6df20f76b8c170e0432f4e990f0159c12265d53aecac33961e62`，同时存在于 `ready.json` 与 owned ledger。
- 额外进程树为 root `61060` → listener `49364`，身份仅判定为绑定本机 8765 的 `python-http-server`；listener 启动于 `2026-09-09T19:43:28.6283357Z`，命令哈希 `148408b402e15e6067cb0df533773c98751f90f5ac965b4726acb3be20bc4257`，不在 `ready.json` 或 owned ledger 中。
- PID `49364` 比 r1 最终源码确认时间早约 3559.83 秒；其进程树与 r1 精确停止的 5 个 Backend/Worker PID 无交集。因此它不是 P6-L1 创建或重启的进程，也不属于本包可清理范围。

### Windows TCP 视图差异

- 完整 `netstat -ano -p tcp` 在诊断前后均列出同一默认 compartment 中的两个 `127.0.0.1:8765` listener：PID `21264` 与 PID `49364`。
- 两次交叉采样中，`Get-NetTCPConnection` 与 `MSFT_NetTCPConnection` 各只返回一条重复端点记录，并在 `21264`、`49364` 之间对调。这说明单条 PowerShell/CIM 结果会折叠重复端点，不能作为完整监听集合。
- 机器仅报告默认 network compartment，因而没有证据支持“两者位于不同网络隔离上下文”的解释。外部只读证据与同端点 socket reuse 加提供器去重相符；本包没有通过侵入性手段断言更底层的 socket option 因果。

### 实际接收与 Demo 健康

- 共进行 48 次受控连接：24 次仅请求 `/health`，24 次仅请求 `/control/state`；没有请求 `/`，没有浏览或保存 `http-server` 目录内容。
- 48/48 个连接的服务端 ESTABLISHED 记录都归属 PID `21264`；PID `49364` 接收数为 0。
- `/health` 24/24 返回 HTTP 200 且 `service=v1-demo`；`/control/state` 24/24 返回 HTTP 200 且 `changed_locator=true`。
- 收口结论：原 Demo 进程确实连续存活并实际服务；额外监听是 r1 之前已经存在的未归属进程。r1 的 Backend/Worker 加载结果不受影响，但 r1 的“监听集合前后不变”证据应收窄为“`Get-NetTCPConnection` 单条提供器视图前后相同”，不能解释为对完整 TCP 表的证明。
- r2 全程停止/启动/重启进程数均为 0，业务写入、消息操作、目录请求及凭据/完整命令输出均为 0；未清理 PID `49364`。

## 接口与 Worker 验证

- `/health` 正常，正式 OpenAPI 已出现全部 7 个目标路径：reports 列表、详情、cases、steps、evidence、export，以及 dashboard。
- 使用一次允许的正常登录执行只读 API 检查：Project 23 报告总数 13；详情 Run 为 `run_7852432fb7b241dabcad111664b9621b`；该报告 cases=1、steps=1、evidence=5；dashboard 返回 `read_only=true`。
- Runner 最终为 ACTIVE/ONLINE、WEB READY、容量 1/1、active Run=0，且数据库/Redis 心跳晚于源码确认时间。
- 最终 RabbitMQ Runner 队列仍为 0，原 DLQ 仍为 messages=1、ready=1、unacked=0。

## 清单与证据

加载前清单已保存：

- `ready-history-pre-P6-L1-r1-20260909T204256150Z.json`，SHA-256 `d431be67d8bf90a0665b117cee11ef44c3768aedd2534305f6ef118d0d94c086`。
- `owned-processes-history-pre-P6-L1-r1-20260909T204256150Z.json`，SHA-256 `47f4ae34d688f276032549e7187c35653ac9349c8270ba402d14a6d46e97b856`。

当前清单已仅更新 Backend/Worker 进程与加载元数据，并同步 `ready.json.owned_processes`：

- `.codex-validation/p5e-services/ready.json`，SHA-256 `89a58ad23680f79b02809a73218582a466c1b11584157d1a88ef7c502615d3eb`。
- `.codex-validation/p5e-services/owned-processes.json`，SHA-256 `2f78160afc4b8c361b00f207526020d58d954cc523d09032b4aae99ec236d0a9`。

独占证据：

- `.codex-validation/p6-l1/load.json`，状态 PASSED，SHA-256 `63b117ba3023e9e36545d581babee769e0fd5bbdb82e8ca4e1d17148ecc336cf`。
- `.codex-validation/p6-l1/verification.json`，32/32 通过，SHA-256 `af484723c0eb2201fe1a3b97d1f6e19876ed13880b8475add5feca5decb1ba6f`；该哈希也已写入 load 记录。
- `.codex-validation/p6-l1/r2-listener-diagnostic.json`，状态 `PASSED_WITH_R1_EVIDENCE_SCOPE_CORRECTION`，SHA-256 `db10d71ced0b32fcf063ab178630b37575b2854a344ed93400898428c449aa6a`。
- 加载工具：`deploy/p6_l1_load.ps1`。
- 只读验证工具：`deploy/p6_l1_readiness.py`。

## 安全计数

- 精确停止旧 Worker PID：`23364, 57108`。
- 精确停止旧 Backend PID：`29460, 22252, 52512`。
- 业务 POST：0；允许的正常登录 POST：1；AI 请求：0。
- 消息 get/publish/ack/redrive：0；Runner 注册：0；容器重启：0；Demo 状态变更：0。

## 未完成项

- 本包无加载阻塞或遗留恢复动作。
- 未归属 PID `49364` 的生命周期与清理不在 P6-L1 权限内；如后续需要处理，必须由主控另行确认归属和授权。本次仅记录，不清理。
- 前端最终审查、真实页面/Chrome 联调、Phase 6 与 V1 最终验收不在本包范围内，由主控继续执行。
