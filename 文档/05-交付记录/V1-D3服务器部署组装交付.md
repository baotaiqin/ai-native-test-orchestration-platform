# V1-D3 服务器部署组装交付

2026-09-11 已由单窗口主控直接完成。

- `deploy/server/` 提供非 root Backend 镜像、Frontend/nginx 镜像、独立 Compose、只读 Fernet key file 挂载、无默认真实密码的配置模板及分离的应用/迁移/初始化命令。
- 只公开 nginx 入口与显式的 Windows Runner RabbitMQ 绑定；MySQL、Redis、MinIO 保持内部网络。
- 合成非敏感环境值的 `docker compose ... config --quiet` 解析通过。未执行镜像拉取、真实 Linux 启动、正式迁移或管理员初始化。
- 代码 Alembic head 已更新为 `20260911_0054`，正式库仍记录为 `20260909_0038`。
