# V1-P1 性能测试主流程交付

## 交付状态

- 已实现：API/Scenario 版本复用，固定迭代、固定 RPS、固定并发/时长与 Step Load，Python HTTP、SSE/NDJSON/LLM 和 JMeter，Warm Up、每迭代/批量 Cleanup，普通与流式指标，确定性 SLA，2～5 Run 对比，PERFORMANCE Slot 投递，spawn 隔离与取消/强停/总超时，跨 Consumer 重建的 completion 结果落盘去重，以及项目页完整入口。
- 基本检查通过：Backend 性能专项 **8 passed**；Runner 性能/协议/消费者/CLI/隔离/Scenario 相关回归 **240 passed**；两端相关 Ruff 通过；Frontend type-check 和隔离输出目录 production build 通过；Alembic 代码与本地开发库均为单一 head `20260913_0071`，且实际表已具备新列。默认 `frontend/dist` 因运行中进程占用未替换，旧入口引用的 35 个资源均仍存在；待服务停止后再执行默认目录构建。
- 真实执行证据：本机 JMeter 5.6.3 对一次性临时 HTTP 服务执行 2 并发、1 预热、4 正式迭代，4 个正式样本全部返回 HTTP 200；临时服务随命令结束，未重启平台服务。
- 待集中验收：独立临时库空库升级/往返，常驻 Runner 重新加载新代码后的 API/Scenario/SSE/JMeter 真实压力矩阵，实时取消/断线时序和浏览器视觉复核。

## 设计边界

性能执行仅复用已审核、固定版本的 API/Scenario；数据源驱动 API、需要服务端 AI 判定的 Scenario、JMeter Multipart、SSE/JMeter Cleanup 保持显式拒绝。Warm Up 只执行不计入统计。Runner 只上传有界单次样本且不上传正文，Backend 重新计算所有汇总指标并判定 SLA，后续 AI 分析不能覆盖原始指标或 SLA 结论。

性能主流程实现已收口。V1 增强能力当时仍需后续实现 Test Plan、Scheduler、CI/CD API/Webhook、AI Performance Analysis、Prometheus 和 Scenario Flow Editor；当前不将它们冒充为本次交付。
