# P5-F 隐私主控审查记录

2026-09-10。当前为缺陷取证，未通过整体隐私验收。依据总规格第28节：日志自动脱敏、API默认不返回明文；保留AI原始响应的审计要求不能替代展示边界。

## 隔离复现

主控新增 `.codex-validation/test_p5f_privacy_review.py`。独立内存SQLite、实际FastAPI路由与序列化，身份依赖替身；仅合成标记值，无正式数据库、网络、AI或真实凭据。测试进程关闭后台Run扫描。

实际结果：2 passed、2 failed，3.74秒。

- 项目外用户读取AI调用历史返回404，没有泄漏合成标记：通过。
- Run事件解析器丢弃未知的password/raw_response字段：通过。此证据限解析函数，不等于所有SSE权限与实时流程已验。
- 项目内只读成员读取 `/api/v1/ai/calls`，`raw_response`、`repair_response`、`parsed_result`、`validation_errors`均原样暴露合成凭据值：失败。当前 `list_ai_calls` 直接序列化持久化AiCallLog，无展示脱敏层。
- `core/logging.py` 的JsonFormatter原样输出消息中的凭据赋值和嵌套token/password值：失败。通用异常日志也使用该formatter，不能依赖每个调用点永远传入安全内容。

测试失败摘要只打印字段名/布尔结论，不打印合成值或原始响应。现有正式日志没有被读取，本报告不声称已经发生真实秘密泄漏。

## 主控待办与当前执行

H5已经完成并验收。Backend当前执行P5F-L1/r1：使用有界递归脱敏处理日志消息/参数/extra/异常及AI历史展示；保留结构化审计标识、模型/版本/Token/成本，原始持久化审计记录不被展示操作改写；识别敏感键、Bearer、凭据赋值、URL认证/敏感查询等，不以清空全部输出冒充修复。本包不重启正式服务，与Frontend H4只读验收独立。修复交付后重新运行本复现及领域测试，再安排服务加载和正式页面只读验证。

主控补充独立测试 `.codex-validation/test_p5f_privacy_edge_review.py`，覆盖顶层日志extra敏感键、转义或不完整的凭据文本以及MySQL/RabbitMQ/Redis连接串。开发中版本的首次结果为8失败4通过，仅为待复验线索，不代表最终交付结论。普通审查意见记录在主控台账，待L1结束后复测最终文件，仍失败再集中派发修订包；运行中不发送追加消息。

AI Prompt输入、Evidence下载/Trace、Runtime快照及完整SSE链仍按矩阵继续核查，不能将上述四项取证扩展为全部安全验收。

最终磁盘验收：L1/r1完成后主控16项复测发现13通过3失败，均为非HTTP服务DSN。主控仅在r1结束后派r2；r2通用URI修复已交付，全量412通过，主控34项组合回归全通过、5文件指纹匹配。原始审计保留、展示副本脱敏和项目权限均通过。正式Backend未重启，仍须加载及正式只读复验，完整P5-F未关闭。
